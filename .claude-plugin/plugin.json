{
  "name": "session-continuity",
  "version": "0.1.0",
  "displayName": "Session Continuity",
  "description": "Git-native, multi-user, drift-aware session-continuity plugin for Claude Code.",
  "author": { "name": "Session Continuity Plugin" },
  "license": "MIT",
  "keywords": ["session-continuity", "ledger", "mcp", "handoff", "resume", "drift"],
  "userConfig": {
    "ledger_backend": {
      "type": "string",
      "title": "Ledger backend",
      "description": "Backend used to store the continuity ledger. 'orphan-branch' keeps ledger records on a dedicated git ref; 'custom-ref' targets a caller-supplied ref. Values are validated at the server boundary.",
      "default": "orphan-branch"
    },
    "ledger_branch": {
      "type": "string",
      "title": "Ledger branch",
      "description": "Name of the git ref that holds the ledger when a git backend is selected.",
      "default": "_ledger"
    },
    "disable_trailer": {
      "type": "boolean",
      "title": "Disable commit trailer",
      "description": "When enabled, the managed commit-msg hook does not append the continuity trailer to commit messages.",
      "default": false
    }
  }
}
