# MCP server audit — evidence base

## Provenance

This file is GENERATED from `docs/audits/2026-08-04-mcp-audit.json`. Every row below was emitted
from that JSON by a script; no defect was retyped by hand.

`docs/audits/2026-08-04-mcp-audit.json` is the authoritative artifact. This markdown is a
navigation index over it and is lossy by construction: it carries one anchor per survivor, not
the full `evidence[]`, and it carries no `mechanism`, `model_impact` or `failure_scenario` text.

Do not trust this markdown over the JSON. Before acting on any defect, open the JSON entry with
the same `id` and read it in full, then open the cited `file:line` yourself. A claim that cannot
be pinned to a line you personally opened is not made.

## Counts

| Population | JSON path | Count |
| --- | --- | --- |
| Findings that survived adversarial verification | `.result.survivors` | 88 |
| Defects the verifiers found on their own | `.result.missed` | 33 |
| Total verified defects | both | 121 |

### Verdict split (survivors only)

- CONFIRMED: 73
- OVERSTATED: 15
- no other verdict value appears; zero findings were refuted.

### Severity

EFFECTIVE severity is `verdict.corrected_severity` when present, falling back to `severity`. The
severity column of the survivor table below is the effective severity.

- effective severity (survivors): critical 10, high 23, medium 35, low 20
- raw `.severity` as first claimed (survivors): critical 15, high 27, medium 36, low 10
- verifier-found severity, parsed from the entry prefix: critical 2, high 2, medium 2, low 3, unrated 24

Criticals: 10 survivors at effective critical, plus 2 carried only in
`.result.missed`, for 12 total. The raw `.severity` field claims
15 survivor criticals; the corrected field is the operative one.

### Dimension

- survivors: reliability 16, security-and-injection 13, skill-server-contract 21, tool-contracts 21, validation-coherence 17
- verifier-found: reliability 5, security-and-injection 6, skill-server-contract 7, tool-contracts 9, validation-coherence 6

## How to read the survivor table

- `id` is the `id` field of the JSON entry and is stable. Cite a defect by this id. Ids are unique
  across both the survivor table and the verifier-found section.
- `severity` is the EFFECTIVE severity defined above, not the raw claimed one.
- `anchor` is the `file:line` of `evidence[0]`, trimmed to path and line. Where `evidence[0]` carries
  no leading `file:line`, the anchor is taken from the first evidence entry that does and the source
  index is stated in the cell. The remaining evidence entries live only in the JSON.
- An OVERSTATED verdict means the finding is real but its first claim was too broad. The verdict cell
  carries the instruction to read `verdict.reasoning` first, because the narrowed claim is the true one.

## Survivors

| id | dimension | severity | verdict | title | anchor |
| --- | --- | --- | --- | --- | --- |
| `resume-brief-consumes-drift` | tool-contracts | critical | CONFIRMED | get_resume_brief destroys the drift snapshot as a side effect, so a second call silently returns a briefing missing the SINCE YOU LEFT section | `src/tools/get-resume-brief.mjs:28` |
| `out-of-scope-silent-wholesale-replace` | tool-contracts | critical | CONFIRMED | update_thread replaces spine.out_of_scope wholesale while its own description teaches scope-carrying merge semantics for the two sibling arrays, silently deleting prior entries | `src/tools/update-thread.mjs:56-58` |
| `record-decision-bypasses-title-cap` | tool-contracts | medium | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | record_decision writes spine.key_decisions[].title with no length gate, bypassing the 120-char cap update_thread enforces on the identical field | `src/tools/record-decision.mjs:68-70` |
| `scope-accepts-nonexistent-criterion` | tool-contracts | critical | CONFIRMED | scope is validated only by regex, so risks and decisions can be filed under a criterion id that does not exist and become permanently invisible in every briefing | `src/schema/patterns.mjs:24` |
| `duplicate-decision-ref-in-spine` | tool-contracts | medium | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | The same decision ref can be stored twice in spine.key_decisions under different scopes and renders twice in the briefing; nothing deduplicates by ref | `src/tools/record-decision.mjs:68` |
| `amend-criteria-oneof-nonconvergent` | tool-contracts | high | CONFIRMED | amend_criteria's oneOf plus params-stripping error formatting produce up to 13 mutually contradictory clauses that never name the offending property, making retry non-convergent | `src/tools/schemas.mjs:72-109` |
| `risk-em-dash-undocumented` | tool-contracts | high | CONFIRMED | open_risks[].text must contain a literal U+2014 em dash with surrounding spaces; the input schema says only minLength 1 and the 831-char description never mentions it | `src/tools/spine-input.mjs:5` |
| `no-tool-returns-a-thread-or-roster` | tool-contracts | medium | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | No tool in the set returns a thread record or resolves a slug to a ULID, and rebuild_index — the tool preflight calls to produce the roster — returns only counts | `src/tools/registry.mjs:19-34` |
| `write-tools-echo-whole-thread` | tool-contracts | medium | CONFIRMED | Every mutating tool echoes the complete thread record; a 43-byte spine edit returns ~1,570 tokens of data the caller already holds | `src/tools/update-thread.mjs:125` |
| `transition-done-preconditions-undocumented` | tool-contracts | medium | CONFIRMED | transition_thread's done path has two hard preconditions stated in neither the schema nor the 95-char description, and the failure never names which criteria are outstanding | `src/tools/transition-thread.mjs:60` |
| `archive-and-reopen-redundant-and-inconsistent` | tool-contracts | medium | CONFIRMED | archive_thread and reopen fully duplicate transition_thread paths while disagreeing with it on validation: a whitespace-only reason is accepted by one and refused by the other | `src/tools/archive-thread.mjs:42` |
| `record-decision-empty-body-accepted` | tool-contracts | medium | CONFIRMED | record_decision accepts empty context, empty outcome and empty option strings, writing a structurally valid but content-free MADR record | `src/tools/record-decision.mjs:92` |
| `record-decision-omits-ref` | tool-contracts | medium | CONFIRMED | record_decision returns number and path but not ref, the only form every downstream tool accepts | `src/tools/record-decision.mjs:67` |
| `spine-decision-title-unchecked` | tool-contracts | medium | CONFIRMED | spine.key_decisions[].title is free text never checked against the decision file it references, so the briefing can attribute wording to a decision that does not contain it | `src/tools/spine-input.mjs:74-83` |
| `write-side-not-idempotent-undocumented` | tool-contracts | medium | CONFIRMED | Every write tool double-writes on retry after an ambiguous failure, and no description says so | `src/tools/record-decision.mjs:56` |
| `recovery-degraded-undocumented` | tool-contracts | medium | CONFIRMED | recovery_degraded rides on every write return with no documented meaning, and it is how an uncommitted write reports success | `src/tools/shared.mjs:15-17` |
| `decision-number-arity-disagrees` | tool-contracts | low | CONFIRMED | The decision number's digit arity disagrees across three layers; at decision 10000 record_decision writes an orphan file and then fails permanently | `src/schema/patterns.mjs:8` |
| `criterion-kind-required-in-one-tool-optional-in-another` | tool-contracts | low | CONFIRMED | criterion `kind` is optional with a silent default in open_thread/create_successor but required in amend_criteria's insert branch | `src/tools/schemas.mjs:48-56` |
| `create-successor-narrower-than-open-thread` | tool-contracts | low | CONFIRMED | create_successor silently drops the slug, vcs_ref and external_refs that open_thread accepts, with no way to set them afterwards | `src/tools/open-thread.mjs:31-39` |
| `replace-scopes-empty-scope-silent-noop` | tool-contracts | low | CONFIRMED | replace_scopes naming a scope that holds nothing is silently accepted as a no-op, so a failed retirement reports success | `src/tools/spine-input.mjs:26-34` |
| `description-budget-misallocated` | tool-contracts | low | CONFIRMED | The 14 descriptions spend their budget unevenly: update_thread's 831 chars omit two rules that guarantee failure, while transition_thread's 95 chars omit a required field | `src/tools/update-thread.mjs:130` (from evidence[1]; evidence[0] carries no file:line) |
| `record-decision-orphan-decision-files` | validation-coherence | critical | CONFIRMED | record_decision writes the decision file to disk before validating the thread record, so every failed call leaves an orphan numbered decision the model believes was never created, and each retry mints a new number | `src/tools/record-decision.mjs:66` |
| `stored-record-readable-but-unwritable` | validation-coherence | high | CONFIRMED | readThread upcasts without validating while writeThread validates, so a stored record that fails threadSchema reads and briefs fine but hard-blocks transition_thread, amend_criteria and record_decision with an error naming a field none of those tools accept | `src/drivers/local-driver.mjs:143-145` |
| `legacy-scope-retirement-silent-noop` | validation-coherence | high | CONFIRMED | A legacy-scoped key_decision produced by the v1 upcast can never be retired: naming 'legacy' in replace_scopes is rejected by a bare regex message, and naming any other scope returns success while silently leaving the entry in place | `src/schema/upcast.mjs:53-59` |
| `record-decision-bypasses-terminal-guard` | validation-coherence | high | CONFIRMED | record_decision mutates a done or abandoned thread's spine and updated_at, while update_thread and amend_criteria both refuse terminal threads | `src/tools/update-thread.mjs:109-111` |
| `spine-caps-absent-from-write-path` | validation-coherence | high | CONFIRMED | None of the ten SPINE_CAPS exist in threadSchema, and assertSpineCaps runs only in update_thread and only against the submitted patch — so over-cap values entering via record_decision or the v1 upcast persist through every later write and are silently ellipsized in the briefing | `src/model/caps.mjs:1-12` |
| `amend-criteria-oneof-error-explosion` | validation-coherence | high | CONFIRMED | amend_criteria's three-branch oneOf emits nine-to-eleven mutually contradictory required-property clauses that never name which op branch failed, and 'must be equal to constant' omits the constant | `src/tools/schemas.mjs:72-109` |
| `record-decision-options-coercion-asymmetry` | validation-coherence | high | CONFIRMED | normalizeOptions normalizes only the string branch; the array branch the skill instructs the model to use is written into the permanent decision record raw, producing '- - keep it', empty bullets and untrimmed text, while a comma-separated string silently collapses to one option | `src/tools/record-decision.mjs:8-14` |
| `decision-title-cap-bypass` | validation-coherence | medium | CONFIRMED | record_decision writes an uncapped title into spine.key_decisions, bypassing the 120-char decisionTitleMaxChars that update_thread enforces, and the briefing then silently truncates it | `src/tools/record-decision.mjs:91` |
| `structured-error-detail-flattened-at-transport` | validation-coherence | medium | CONFIRMED | Every error is flattened to {error: <JS class name>, message} in an isError tool result, discarding CapViolationError.fields, ToolValidationError.errors and SchemaValidationError.errors, and giving bootstrap failures the same shape as input-validation failures | `bin/ledger-server.mjs:57` |
| `enum-and-const-allowed-values-dropped` | validation-coherence | medium | CONFIRMED | formatValidationErrors discards ajv's params, so every enum rejection reads 'must be equal to one of the allowed values' and every const rejection reads 'must be equal to constant' with the permitted values never stated | `src/schema/validators.mjs:10` |
| `risk-sentence-shape-not-in-schema` | validation-coherence | medium | CONFIRMED | open_risks[].text must match a spaced em-dash two-clause regex enforced only in a handler; neither the input schema nor the tool description mentions it, so any caller not driven by the debrief skill fails on first use | `src/tools/spine-input.mjs:5` |
| `abandoned-reason-emptiness-divergence` | validation-coherence | medium | CONFIRMED | archive_thread stores a whitespace-only abandoned_reason that transition_thread refuses for the identical field, and transition_thread reports a supplied-but-blank value as 'required' | `src/tools/archive-thread.mjs:42` |
| `archive-thread-misleading-transition-hint` | validation-coherence | medium | CONFIRMED | archive_thread appends a fixed 'a blocked thread must first go blocked -> paused' hint to every illegal-transition refusal, so archiving an already-terminal thread instructs the model to perform an unrelated and itself-illegal transition | `src/tools/archive-thread.mjs:12-16` |
| `thread-slug-uniqueness-and-shape-unenforced` | validation-coherence | medium | CONFIRMED | Thread slugs are neither unique nor pattern-checked at any layer: an explicit slug is stored verbatim while a derived slug is normalized, and duplicate slugs are silently collapsed by the by-slug index so branch reattach resolves the wrong thread | `src/tools/open-thread.mjs:33` |
| `non-latin-title-hard-fails-slug-derivation` | validation-coherence | low | CONFIRMED | toSlug strips every non-ASCII character, so any fully non-Latin thread title fails open_thread with a bare Error and a constraint stated nowhere in the schema or description | `src/model/thread.mjs:10-15` |
| `decision-ref-digit-width-inconsistency` | validation-coherence | low | CONFIRMED | Decision refs are pinned to exactly four digits in the thread schema and every input schema but read_decision accepts four-or-more and nextDecisionNumber grows past four, so a ledger past 9999 decisions writes the file then fails the thread write | `src/schema/patterns.mjs:8` |
| `iso-timestamp-pattern-unanchored` | validation-coherence | low | CONFIRMED | ISO_TIMESTAMP_PATTERN has no trailing anchor, so created_at and updated_at accept any trailing garbage and any impossible date component | `src/schema/patterns.mjs:3-4` |
| `debrief-thread-id-has-no-recovery-path` | skill-server-contract | high | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | debrief's only source of thread_id is a SessionStart text injection that no tool can re-fetch, so a compacted or long session cannot address any write tool | `skills/debrief/SKILL.md:11` |
| `stop-gate-never-arms-in-a-resumed-session` | skill-server-contract | critical | CONFIRMED | The Stop hook is the only mechanism forcing a debrief, and it fires only on the active-thread pointer, which nothing in the documented preflight flow ever sets | `hooks/lib/stop.mjs:101-104` |
| `global-rule-directs-writes-to-a-ledger-the-server-never-reads` | skill-server-contract | medium | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | The user-global continuity rule points the model at <repo>/.claude/ledger/ markdown files, which is not a guarded path and not a path any tool reads | `/Users/satanshumishra/Documents/DevLabs/.windful-ocean/.claude/rules/common/continuity-ledger.md:7` |
| `replace-scopes-silently-deletes-decisions-recorded-minutes-earlier` | skill-server-contract | high | CONFIRMED | debrief step 5's replace_scopes instruction, followed literally without also submitting key_decisions, deletes the spine links to the decisions step 3 just created | `skills/debrief/SKILL.md:32` |
| `debrief-step-4-child-thread-escape-hatch-is-unreachable` | skill-server-contract | high | CONFIRMED | debrief step 4 instructs promoting work to a child thread, but no tool that creates a thread is in the skill's allowed-tools or prose, and the detour-nesting refusal that forces the choice is unstated | `skills/debrief/SKILL.md:24` |
| `amend-criteria-oneof-error-names-the-wrong-missing-fields` | skill-server-contract | high | CONFIRMED | A malformed amend_criteria operation returns an ajv oneOf error listing required properties from all three branches, actively directing the model to add fields the operation it intends must not have | `src/tools/schemas.mjs:72-109` |
| `bind-branch-sets-the-pointer-on-a-non-active-thread-stranding-the-stop-gate` | skill-server-contract | medium | CONFIRMED | bind_branch writes the active-thread pointer regardless of thread status, but transition_thread and archive_thread clear it only when the stored status is 'active', so a bound paused thread leaves a pointer that debrief cannot clear | `src/tools/bind-branch.mjs:8-14` |
| `get-resume-brief-arms-an-unconditional-print-obligation-the-skill-never-mentions` | skill-server-contract | low | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | Calling get_resume_brief writes a pledge that makes the Stop hook block the turn until the exact briefing text appears in an assistant message, and the skill never says the call itself creates that obligation | `src/tools/get-resume-brief.mjs:30-34` |
| `record-decision-accepts-empty-context-and-outcome` | skill-server-contract | medium | CONFIRMED | record_decision requires context and outcome but enforces no minLength, so an empty-string decision record is written, committed and linked into the spine as if complete | `src/tools/record-decision.mjs:92` |
| `read-decision-nnnn-is-a-zero-padded-string-not-the-number-the-skill-names` | skill-server-contract | low | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | preflight tells the model to call read_decision 'with that number', but the parameter is named nnnn and must be a string of at least four digits, so the natural JSON emission is rejected twice | `skills/preflight/SKILL.md:26` |
| `spine-caps-and-hard-refusal-semantics-are-stated-nowhere` | skill-server-contract | medium | CONFIRMED | Ten character/count caps gate the debrief's largest call, the skill states none of them, and the global rule promises graceful demotion where the server hard-throws and discards the whole call | `src/model/caps.mjs:1-12` |
| `decision-scope-defaults-hide-the-decision-from-the-next-briefing` | skill-server-contract | medium | CONFIRMED | record_decision scopes a decision to the current criterion by default and the skill never mentions the scope parameter, so a thread-wide decision recorded at debrief is invisible in the very next briefing | `src/tools/record-decision.mjs:52-55` |
| `preflight-step-1-and-debrief-step-7-rebuild-nothing-observable` | skill-server-contract | low | CONFIRMED | rebuild_index returns only counts and every mutating tool already reindexes, so both skills' explicit rebuild steps refresh a file the model cannot read and that was already current | `skills/preflight/SKILL.md:15` |
| `preflight-reconcile-is-a-no-op-that-still-commits` | skill-server-contract | low | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | preflight step 3 mandates reconcile, but no skill ever creates a binding and SessionStart already reconciled, so the call reliably observes nothing while paying a commit and a full reindex | `skills/preflight/SKILL.md:22` |
| `debrief-ordering-is-convention-and-a-partial-run-leaves-a-committed-lie` | skill-server-contract | medium | CONFIRMED | Only the terminal-refusal in update_thread/amend_criteria enforces any of debrief's ordering; a run that dies after the session log and decisions leaves a committed session with a stale spine and no incompleteness marker | `skills/debrief/SKILL.md:27` |
| `global-continuity-rule-contradicts-the-server-on-eight-points` | skill-server-contract | medium | CONFIRMED | The always-loaded continuity-ledger rule describes a different system: different skill names, file layout, cap units, decision format, auto-transition, criteria immutability, staleness scan and supersession workflow | `continuity-ledger.md:3` |
| `thinness-invariant-forbids-the-skill-from-stating-the-transition-it-must-perform` | skill-server-contract | medium | CONFIRMED | A test forbids the string 'active -> paused' in either SKILL.md, structurally guaranteeing that the model can never be told the precondition its step-6 call requires | `test/unit/skills/skill-invariants.mjs:8-14` |
| `empty-spine-renders-headed-but-empty-sections-the-model-must-print-verbatim` | skill-server-contract | medium | CONFIRMED | WHY and NEXT STEP are rendered unconditionally, so a never-debriefed thread produces a briefing of empty headings that the Stop hook forces the model to print verbatim | `src/render/briefing.mjs:50-52` |
| `debrief-payload-peaks-exactly-when-context-is-most-exhausted` | skill-server-contract | low | CONFIRMED | The largest, most deeply nested and most heavily validated call of the whole protocol is scheduled last, behind fourteen distinct refusal sites, at the point of maximum context pressure | `skills/debrief/SKILL.md:27-32` |
| `inserted-criterion-ids-are-server-assigned-and-only-in-the-response` | skill-server-contract | medium | CONFIRMED | debrief step 5 must toggle criteria by id, but the id of a criterion step 4 inserted is assigned server-side, does not match its position, and the skill never says to read it from the amend response | `src/tools/amend-criteria.mjs:60-66` |
| `roster-truncates-next-step-to-120-chars-against-a-500-char-cap` | skill-server-contract | low | CONFIRMED | next_step accepts 500 characters but the roster the model reads at the next session start truncates it to 120, and neither skill says so | `src/model/caps.mjs:3` |
| `worktree-nuked-on-every-init` | reliability | critical | CONFIRMED | GitRefDriver.init() unconditionally `rm -rf`s the shared ledger worktree, so every new server or CLI process destroys another live session's uncommitted writes and all index files | `src/tools/context.mjs:10` |
| `v1-upcast-produces-unwritable-records` | reliability | medium | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | upcastThread produces v2 records that fail assertValidThread, making legacy threads readable through every read path but permanently un-writable through every mutating tool | `src/schema/upcast.mjs:61-75` |
| `worktree-lock-fails-open-and-covers-nothing` | reliability | high | CONFIRMED | The worktree lock is advisory-only: it is released before any write happens, and on timeout acquireWorktreeLock returns false while #ensureWorktree proceeds to `rm -rf` anyway | `src/drivers/git-ref-driver.mjs:64` |
| `duplicate-decision-numbers-no-reservation` | reliability | critical | CONFIRMED | nextDecisionNumber is a read-max-then-increment with no reservation or exclusive create, so concurrent record_decision calls mint the same number and readDecision then returns whichever file readdir happens to yield first | `src/drivers/local-driver.mjs:189-200` |
| `no-rollback-across-multi-step-tool-writes` | reliability | high | CONFIRMED | transition_thread, archive_thread, reopen and record_decision perform 3-4 independent durable writes with no rollback, so a failure at any later step leaves the earlier writes applied while the tool reports failure | `src/tools/transition-thread.mjs:39-46` |
| `corrupt-thread-json-bricks-every-mutating-tool` | reliability | high | CONFIRMED | JSON.parse is unguarded and listThreads reads every thread file on every mutating call, so one truncated thread JSON makes all twelve mutating tools fail with an error that names no file | `src/drivers/local-driver.mjs:55-64` |
| `recovery-degraded-is-a-constant-false-on-git` | reliability | medium | CONFIRMED | recovery_degraded can only ever be true on the non-git LocalDriver; GitRefDriver throws instead of degrading, so the flag every tool returns is a hardcoded false on the primary backend and carries no cause or remedy in either case | `src/tools/shared.mjs:15-17` |
| `uncommitted-write-guard-cleared-on-commit-failure` | reliability | high | CONFIRMED | GitRefDriver.commit clears the #uncommittedWrite guard in a `finally`, so after a failed commit the very next worktree resolution is free to `rm -rf` and re-provision, destroying the write the guard existed to protect | `src/drivers/git-ref-driver.mjs:206` |
| `record-decision-bypasses-every-spine-cap` | reliability | medium | CONFIRMED | record_decision appends to spine.key_decisions without calling assertSpineCaps, and thread.schema.mjs enforces no length limit, so the documented 120-char decision title cap is enforced on update_thread and silently unenforced on the tool that actually creates decisions | `src/tools/record-decision.mjs:68-76` |
| `listthreads-skips-upcast-index-built-from-v1-shape` | reliability | low | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | readThread upcasts v1 records but listThreads does not, so the derived index and get_resume_brief's children/predecessor lookups consume raw legacy records the current schema no longer describes | `src/drivers/local-driver.mjs:144` |
| `per-call-full-index-rebuild-and-git-spawns` | reliability | low | CONFIRMED | Every mutating tool call re-reads every thread and binding file, rewrites four index files and spawns five git subprocesses, making per-call cost O(threads + bindings) and per-session cost quadratic | `src/tools/shared.mjs:19-22` |
| `session-event-filename-collision-silently-overwrites` | reliability | medium | CONFIRMED | appendSessionEvent derives its filename from timestamp plus actor and writes with a replacing rename, so two events in the same millisecond from the same actor silently destroy one of them | `src/drivers/local-driver.mjs:244-249` |
| `atomic-write-is-not-crash-durable-and-leaks-tmp-into-commits` | reliability | low | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | atomicWrite never fsyncs the file or the directory, so a crash can leave a renamed-but-empty record, and its `.tmp-<ulid>` files land inside directories that `git add -A` stages wholesale | `src/util/atomic-write.mjs:14-19` |
| `briefing-pledge-destroyed-by-its-own-context-build` | reliability | high | CONFIRMED | `ledger-cli briefing-pledge` builds a context whose init() `rm -rf`s the worktree containing the gitignored pledge file it is about to read, so on git-backed projects the pledge reads back as null | `bin/ledger-cli.mjs:53` |
| `update-ref-without-compare-and-swap` | reliability | low | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | Every ledger ref advance uses `update-ref <ref> <sha>` with no expected-old-value, so a concurrent session's ledger commit is silently dropped from the ref while its files remain in the shared worktree | `src/drivers/git-ref-driver.mjs:393` |
| `no-tests-for-any-of-these-failure-modes` | reliability | low | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | The failure modes above have no test coverage: no concurrency test, no corrupt-record test, no commit-failure test, no git-backed CLI-pledge test, no v1-upcast-rejection test | `test/unit/cli/briefing-pledge.test.mjs:7-12` (from evidence[1]; evidence[0] carries no file:line) |
| `briefing-verbatim-prompt-injection` | security-and-injection | critical | CONFIRMED | Every string in the preflight briefing is interpolated raw with no escaping, fencing or provenance marking, so a newline in a ledger field forges server-authored headings in a string the skill orders the model to print verbatim as authoritative context | `src/render/briefing.mjs:45` |
| `sessionstart-roster-injection-no-user-action` | security-and-injection | critical | CONFIRMED | formatRoster interpolates unbounded, unescaped ledger title/slug/next_step into SessionStart additionalContext, injecting hostile ledger text into every session before the user types anything | `hooks/lib/roster.mjs:33` |
| `sync-merge-x-theirs-silent-overwrite` | security-and-injection | critical | CONFIRMED | Automatic ledger sync merges the remote ledger with -X theirs and then force-pushes, so a teammate's ledger silently overwrites the local user's conflicting thread state with no report to the model or the user | `src/drivers/git-ref-driver.mjs:488-491` |
| `no-read-side-record-validation` | security-and-injection | high | CONFIRMED | readThread only upcasts and listThreads does not even do that, so every write-side cap and pattern is void for a ledger record authored elsewhere and pulled in via git | `src/drivers/local-driver.mjs:143-145` |
| `corrupt-json-bricks-every-tool` | security-and-injection | high | CONFIRMED | readJsonOrNull calls JSON.parse outside its try block, so one malformed file anywhere in the ledger makes every reindexing tool throw a SyntaxError that names no file | `src/drivers/local-driver.mjs:55-64` |
| `bind-branch-repo-unvalidated-cwd` | security-and-injection | high | CONFIRMED | bind_branch accepts any string as `repo` and reconcile uses it directly as a git process cwd with no per-binding error handling, so one stale or foreign path aborts the whole reconcile with the misleading message "spawn git ENOENT" | `src/tools/bind-branch.mjs:28` |
| `decision-number-poison-partial-write` | security-and-injection | high | CONFIRMED | nextDecisionNumber pads to four digits but never bounds the value, so a single decisions/ filename with a five-or-more-digit prefix permanently breaks record_decision after the decision file has already been written | `src/drivers/local-driver.mjs:16` |
| `worktree-rm-rf-on-every-context-build` | security-and-injection | high | CONFIRMED | Every driver init recursively deletes the ledger worktree before re-adding it, and hook-spawned CLI processes do this concurrently with the long-lived MCP server whose writes are not covered by the worktree lock | `src/drivers/git-ref-driver.mjs:354-359` |
| `no-verify-commits-plus-automatic-push` | security-and-injection | high | CONFIRMED | Ledger commits are made with --no-verify and pushed to origin automatically at session start and session end, so free-form model-authored text bypasses any repo secret-scanning hook and reaches the shared remote with no user gate | `src/drivers/git-ref-driver.mjs:390` |
| `ledger-tools-hard-allowed-in-pretooluse` | security-and-injection | medium | CONFIRMED | The PreToolUse hook returns permissionDecision 'allow' for any tool whose name matches the ledger prefix, removing user confirmation from all fourteen write tools and keying the decision on an attacker-influenceable tool name | `hooks/lib/pre-tool-use.mjs:156-160` |
| `env-user-config-unvalidated-at-boundary` | security-and-injection | medium | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | LEDGER_BACKEND and LEDGER_BRANCH are passed from user_config into ref names and host-repo git config with only a non-empty check, and a bad LEDGER_BACKEND fails every one of the fourteen tools with an opaque bare Error | `.mcp.json:6-9` |
| `unbounded-session-log-and-actor` | security-and-injection | medium | CONFIRMED | append_session_event caps neither actor nor body, so an oversized actor produces an ENAMETOOLONG filesystem error and an oversized body is committed and pushed with no limit | `src/tools/append-session-event.mjs:22-26` |
| `local-paths-committed-to-shared-ledger` | security-and-injection | low | OVERSTATED — read `verdict.reasoning` in the JSON before acting on this row; the narrowed claim is the true one | binding.repo stores a machine-local absolute path and is committed and pushed to the shared ledger ref, leaking local usernames and directory layout to everyone with repo access | `src/tools/bind-branch.mjs:28` |

## Verifier-found defects

`.result.missed` holds 33 plain strings, not objects, so these carry no `id`, no `verdict` and no
structured `evidence[]` in the artifact. The ids below are assigned here by array position
(`missed-01` is `.result.missed[0]`) and are stable for that reason. Dimension and severity are parsed
from the leading `[dimension] SEVERITY` prefix; entries whose prefix names no severity are recorded as
`unrated` rather than assigned one. Each entry is reproduced verbatim; anchors are every `file:line`
found in its prose, in order of appearance.

### missed-01

- dimension: tool-contracts
- severity: critical
- source: `.result.missed[0]`
- anchors: `src/tools/record-decision.mjs:42-51`, `src/tools/update-thread.mjs:109`, `src/tools/amend-criteria.mjs:106`

[tool-contracts] CRITICAL — record_decision has no terminal-thread guard while its two sibling spine writers do, so a done/abandoned thread's spine keeps mutating. src/tools/record-decision.mjs:42-51 checks only existence and never imports isTerminal, in contrast to src/tools/update-thread.mjs:109 and src/tools/amend-criteria.mjs:106 which both refuse a terminal thread. Verified: on a thread transitioned to done, record_decision was ACCEPTED and appended {"ref":"0001-post-mortem","title":"Written after done","scope":"c1"} to spine.key_decisions, while update_thread on the same thread returned 'cannot mutate a terminal (done) thread'. The model is taught 'terminal-refused' by two descriptions and gets the opposite from a third.

### missed-02

- dimension: tool-contracts
- severity: critical
- source: `.result.missed[1]`
- anchors: `src/tools/bind-branch.mjs:14`, `src/tools/transition-thread.mjs:42`, `src/tools/archive-thread.mjs:25`, `hooks/lib/stop.mjs:101-105`, `src/model/fsm.mjs:13`

[tool-contracts] CRITICAL — bind_branch sets the active-thread pointer for a thread of ANY status, and no tool can clear a pointer whose thread is not 'active', which deadlocks session end. src/tools/bind-branch.mjs:14 calls writeActiveThread unconditionally after only an existence check (:8-11). Both clear paths are gated on the thread already being active: src/tools/transition-thread.mjs:42 (`thread.status === 'active' && ...`) and src/tools/archive-thread.mjs:25. hooks/lib/stop.mjs:101-105 blocks session end whenever the pointer file is non-null, with the message 'thread <id> is still active'. Verified: bind_branch on a paused thread set the pointer; the thread's status stayed paused; archive_thread then abandoned the thread and left the pointer set; transition_thread paused->paused is illegal (src/model/fsm.mjs:13). There is no in-protocol remedy.

### missed-03

- dimension: tool-contracts
- severity: high
- source: `.result.missed[2]`
- anchors: `src/tools/transition-thread.mjs:30`, `src/model/fsm.mjs:14`

[tool-contracts] HIGH — transition_thread silently erases blocked_by on the blocked -> paused move, destroying the only record of the blocker. src/tools/transition-thread.mjs:30 sets `blocked_by: to === 'blocked' ? args.blocked_by : null` for every target, and the session event written at :45 is only `Transition blocked -> paused` — the blocker text is nowhere else. src/model/fsm.mjs:14 makes blocked -> paused a first-class legal move, and neither the schema (:53-63) nor the 95-char description (:52) warns that it is destructive.

### missed-04

- dimension: tool-contracts
- severity: high
- source: `.result.missed[3]`
- anchors: `src/tools/open-thread.mjs:33`, `src/schema/thread.schema.mjs:59`, `src/tools/record-decision.mjs:90`, `src/drivers/local-driver.mjs:15`, `src/index/rebuild-index.mjs:34-35`, `src/tools/open-thread.mjs:20`, `src/drivers/git-ledger.mjs:26-31`

[tool-contracts] HIGH — open_thread's slug is unconstrained free text, unlike every other slug in the system, and flows into the by-slug index key and the git commit subject. src/tools/open-thread.mjs:33 declares `slug: { type: 'string', minLength: 1 }` and src/schema/thread.schema.mjs:59 is the same, whereas record_decision requires '^[a-z0-9][a-z0-9-]*$' (src/tools/record-decision.mjs:90) and LocalDriver.writeDecision enforces SLUG_PATTERN (src/drivers/local-driver.mjs:15, :206). Verified: open_thread with slug 'Bad Slug/With Spaces\nand-newline' was ACCEPTED, became a literal by-slug index key (src/index/rebuild-index.mjs:34-35), and is interpolated into the commit message at src/tools/open-thread.mjs:20, which src/drivers/git-ledger.mjs:26-31 accepts as long as it is non-empty.

### missed-05

- dimension: tool-contracts
- severity: medium
- source: `.result.missed[4]`
- anchors: `src/render/briefing.mjs:119-127`, `src/model/selection.mjs:39`, `src/render/briefing.mjs:107-114`

[tool-contracts] MEDIUM — the briefing files thread-scoped standing decisions under the heading 'DECIDED ON THIS STEP', misattributing them to the current criterion. src/render/briefing.mjs:119-127 renders one flat list of every entry whose scope is in visibleScopes, and src/model/selection.mjs:39 always puts THREAD_SCOPE in that set. The risks block does the opposite and explicitly separates standing items under a 'Standing:' subheading (src/render/briefing.mjs:107-114). A decision deliberately promoted to thread scope is therefore read back by the next session as decided on this step.

### missed-06

- dimension: tool-contracts
- severity: medium
- source: `.result.missed[5]`
- anchors: `src/tools/update-thread.mjs:70-76`, `src/tools/spine-input.mjs:94-97`

[tool-contracts] MEDIUM — the 'a decision record is its single home' rule is enforced in one direction only, so ordering decides whether identical content is accepted. src/tools/update-thread.mjs:70-76 runs assertNoRestatedDecision only when the caller submits out_of_scope, and compares against the already-merged key_decisions. src/tools/record-decision.mjs imports nothing from spine-input except assertWritableScope (:4), so recording a decision whose title restates an existing out_of_scope entry is never checked. The matcher itself is a 24-char substring heuristic (src/tools/spine-input.mjs:94-97) that can also refuse legitimate distinct entries.

### missed-07

- dimension: tool-contracts
- severity: low
- source: `.result.missed[6]`
- anchors: `src/tools/record-decision.mjs:68`, `src/drivers/local-driver.mjs:189-199`

[tool-contracts] LOW — record_decision's ref-dedup guard is dead code in normal operation and gives false retry assurance. src/tools/record-decision.mjs:68 tests `thread.spine.key_decisions.some((d) => d.ref === ref)`, but ref is built at :67 from `nnnn` which src/drivers/local-driver.mjs:189-199 always returns as max+1 of the decisions directory — so the ref is new on every call and the branch can only fire if a decision file was deleted out of band. This is precisely why an ambiguous-failure retry duplicates the record rather than being absorbed.

### missed-08

- dimension: tool-contracts
- severity: low
- source: `.result.missed[7]`
- anchors: `src/tools/transition-thread.mjs:60`

[tool-contracts] LOW — transition_thread accepts closure_statement on non-done targets and drops it with no error. The field is declared unconditionally at src/tools/transition-thread.mjs:60, so additionalProperties:false does not reject it, and :32 preserves the prior value for any target other than done. A caller that pauses a thread with a closure sentence gets a success return whose thread record does not contain it.

### missed-09

- dimension: tool-contracts
- severity: low
- source: `.result.missed[8]`
- anchors: `src/tools/archive-thread.mjs:35`

[tool-contracts] LOW — archive_thread's description makes an unconditional claim its code does not honor. src/tools/archive-thread.mjs:35 says it 'clears the active pointer', but :25 clears it only when `thread.status === 'active'` and the pointer matches. Verified: archiving a paused thread whose pointer had been set by bind_branch left the pointer in place while the tool reported success.

### missed-10

- dimension: validation-coherence
- severity: unrated
- source: `.result.missed[9]`
- anchors: `src/tools/transition-thread.mjs:39`, `src/util/active-thread.mjs:26-27`, `src/tools/archive-thread.mjs:24`, `src/tools/reopen.mjs:28`, `src/tools/open-thread.mjs:18`

[validation-coherence] No transaction boundary in transition_thread either — the same defect class as the orphan-decision finding, but it corrupts the FSM instead of the decision numbering. `await driver.writeThread(candidate)` at src/tools/transition-thread.mjs:39 durably persists the new status, and only THEN come the active-pointer writes (:40-44, which call writeActiveThread/readActiveThread/clearActiveThread — all of which throw when CLAUDE_PLUGIN_DATA is unset for a non-git project, src/util/active-thread.mjs:26-27) and `await driver.appendSessionEvent(...)` at :45. I reproduced it by making appendSessionEvent throw: the tool reported `Error | disk full`, the stored record was already `paused`, and the natural retry returned `transition_thread: illegal transition paused -> paused` — the exact error string of known incident #2, reached with no skill bug involved. The same write-then-side-effect ordering exists at src/tools/archive-thread.mjs:24 before :28, src/tools/reopen.mjs:28 before :29-30, and src/tools/open-thread.mjs:18 before :19.

### missed-11

- dimension: validation-coherence
- severity: unrated
- source: `.result.missed[10]`
- anchors: `src/tools/open-thread.mjs:18`, `src/util/active-thread.mjs:27`, `src/index/rebuild-index.mjs:34-35`

[validation-coherence] open_thread's partial write compounds the slug-collision defect into duplicate ghost threads. src/tools/open-thread.mjs:18 writes the thread, then :19 calls writeActiveThread, which throws from src/util/active-thread.mjs:27 whenever CLAUDE_PLUGIN_DATA is unset on a non-git project. I reproduced two retries of the same `open_thread({title:'Ghost'})` call: all three attempts reported an error to the caller, and the threads directory held three records — two of them sharing slug 'ghost' — which src/index/rebuild-index.mjs:34-35 then silently collapses to a single by-slug entry. The model is told the thread was never created while two unreachable duplicates exist.

### missed-12

- dimension: validation-coherence
- severity: unrated
- source: `.result.missed[11]`
- anchors: `src/drivers/local-driver.mjs:143-145`, `src/drivers/local-driver.mjs:155-164`, `src/index/rebuild-index.mjs:41-44`, `src/model/selection.mjs:9`, `src/tools/get-resume-brief.mjs:19-27`, `src/drivers/local-driver.mjs:166-168`

[validation-coherence] Two divergent read paths over the same thread files: readThread upcasts, listThreads does not. src/drivers/local-driver.mjs:143-145 pipes through upcastThread; src/drivers/local-driver.mjs:155-164 calls readJsonOrNull directly and returns the raw record with no upcast and no validation. I reproduced this on one v1 file: listThreads returned `schema_version: 1` with criterion `{"text":"do it","done":false}` and `open_risks: ["plain string risk"]`, while readThread on the same id returned `schema_version: 2` with `{"id":"c1","text":"do it","done":false,"kind":"planned","struck_by":null}`. The consumers of the un-upcast form are exactly the derived-state producers: src/index/rebuild-index.mjs:41-44 reads `thread.spine.next_step` (absent in v1, silently becomes '') and calls criteriaProgress, which reads `c.struck_by` and `c.kind` at src/model/selection.mjs:9 and :27-31 — fields a v1 criterion does not have — and src/tools/get-resume-brief.mjs:19-27 builds the children/predecessor blocks from the same raw list. The roster's counts are computed from fields that only exist after an upcast that never ran. The identical read/write asymmetry exists for bindings: readBinding at src/drivers/local-driver.mjs:166-168 never validates while writeBinding at :170-176 calls assertValidBinding.

### missed-13

- dimension: validation-coherence
- severity: unrated
- source: `.result.missed[12]`
- anchors: `src/tools/amend-criteria.mjs:50-56`, `src/tools/schemas.mjs:48-56`, `src/model/thread.mjs:17-35`, `src/schema/thread.schema.mjs:64-78`, `src/model/selection.mjs:27-30`, `src/render/briefing.mjs:41-42`

[validation-coherence] The detour-nesting invariant lives in exactly one handler and is trivially bypassed at creation. src/tools/amend-criteria.mjs:50-56 refuses inserting a detour while another live detour is open, but criteriaCreateItem (src/tools/schemas.mjs:48-56) admits `kind: 'detour'` and neither src/model/thread.mjs:17-35 nor src/schema/thread.schema.mjs:64-78 has any nesting check. I reproduced it: `open_thread({title:'Detours', completion_criteria:[{text:'a',kind:'detour'},{text:'b',kind:'detour'}]})` succeeded with c1:detour,c2:detour, and the very next amend_criteria detour insert was refused with `criterion "c1" is an open detour and detours do not nest` — permanently, since neither detour can be removed. The same record also breaks the briefing header: criteriaProgress counts only non-detour criteria as `total` (src/model/selection.mjs:27-30), so the rendered header at src/render/briefing.mjs:41-42 reads '0 of 0 done' for a thread with two open items, and I saw `"done":0,"total":0,"detours_open":2` written into the resumable index.

### missed-14

- dimension: validation-coherence
- severity: unrated
- source: `.result.missed[13]`
- anchors: `src/tools/transition-thread.mjs:60`, `src/model/dod.mjs:11-14`, `src/schema/thread.schema.mjs:95`, `dod.mjs:13`

[validation-coherence] closure_statement has three different emptiness definitions across the layers, the mirror image of the abandoned_reason divergence but with the CORRECT wording already present. src/tools/transition-thread.mjs:60 declares `closure_statement: { type: 'string' }` — no minLength, so '' passes ajv; src/model/dod.mjs:11-14 rejects it with `closure_statement must be non-empty for done` after trimming; src/schema/thread.schema.mjs:95 is `{ type: ['string','null'] }` and enforces neither. That dod.mjs:13 message is exactly the phrasing transition_thread fails to use twenty lines earlier at :21 and :24 ('blocked_by is required' / 'abandoned_reason is required'), so the same file family states the same class of failure two incompatible ways.

### missed-15

- dimension: validation-coherence
- severity: unrated
- source: `.result.missed[14]`
- anchors: `src/tools/spine-input.mjs:5`, `src/schema/thread.schema.mjs:12-21`, `src/drivers/local-driver.mjs:148`, `src/schema/upcast.mjs:38-45`, `src/tools/update-thread.mjs:41-47`

[validation-coherence] The RISK_SENTENCE constraint has the same one-layer-only structure as SPINE_CAPS, and the v1 upcast manufactures records that violate it permanently. src/tools/spine-input.mjs:5 and :58-64 are the only enforcement; src/schema/thread.schema.mjs:12-21 declares risk text as `{ type: 'string', minLength: 1 }` with no pattern, so assertValidThread (src/drivers/local-driver.mjs:148) accepts anything. src/schema/upcast.mjs:38-45 wraps every v1 risk string as `{ text: item, scope: THREAD_SCOPE, refs: [] }` with no shape check at all — I saw `open_risks: ['plain string risk']` become a stored risk with no em dash. Because normalizeRisks runs only over `spinePatch.open_risks` (src/tools/update-thread.mjs:41-47), such a risk reads, briefs and survives every subsequent write, while the identical text submitted by the model is rejected. Same signature as the caps finding, different constraint.

### missed-16

- dimension: skill-server-contract
- severity: unrated
- source: `.result.missed[15]`
- anchors: `skills/debrief/SKILL.md:27`, `src/tools/update-thread.mjs:137-148`, `src/schema/validators.mjs:9-11`

[skill-server-contract] skills/debrief/SKILL.md:27 — step 5 enumerates active_goal, next_step, last_session, open_risks, out_of_scope and replace_scopes as if they were peers, but src/tools/update-thread.mjs:137-148 nests the first five under a `spine` object while replace_scopes (:149) and completion_criteria (:150) are top-level; the skill never writes the word `spine` or `completion_criteria` anywhere. I ran the flat emission against the live registry: callTool('update_thread', {thread_id, active_goal:'g', next_step:'n', last_session:'l'}) returns exactly '(root) must NOT have additional properties; (root) must NOT have additional properties; (root) must NOT have additional properties' — three identical, anonymous clauses, because src/schema/validators.mjs:9-11 discards e.params and so never names the offending property. This is the known record_decision incident's exact shape (an error that cannot distinguish 'wrong nesting' from 'unknown field') on the protocol's largest call.

### missed-17

- dimension: skill-server-contract
- severity: unrated
- source: `.result.missed[16]`
- anchors: `skills/debrief/SKILL.md:32`, `src/tools/schemas.mjs:39-46`

[skill-server-contract] skills/debrief/SKILL.md:32 — replace_scopes is described as a flat list ('name every scope whose risks or decisions you are restating'), but src/tools/schemas.mjs:39-46 replaceScopesInput is an object with only `open_risks` and `key_decisions` keys. Verified against the registry: {thread_id, replace_scopes:['c3']} returns '/replace_scopes must be object', and the string form returns the same. The per-field keying — which is also what makes the scoped-deletion hazard possible — is stated nowhere in the skill.

### missed-18

- dimension: skill-server-contract
- severity: unrated
- source: `.result.missed[17]`
- anchors: `skills/preflight/SKILL.md:18`, `src/index/rebuild-index.mjs:36-38`, `src/tools/registry.mjs:19-34`, `src/tools/get-resume-brief.mjs:46`, `hooks/lib/roster.mjs:33`

[skill-server-contract] skills/preflight/SKILL.md:18 — the documented `/preflight <slug>` path has no server-side resolution. src/index/rebuild-index.mjs:36-38 and :63 build and persist a `by-slug` map of slug -> thread id, but no tool in src/tools/registry.mjs:19-34 exposes it, and the only tool that consumes a thread requires a ULID (src/tools/get-resume-brief.mjs:46, pattern ULID_PATTERN). So when the user names a thread by slug, the model must find the id in the SessionStart prose (hooks/lib/roster.mjs:33) or it cannot brief at all — an index the server maintains specifically for this lookup is unreachable from the skill.

### missed-19

- dimension: skill-server-contract
- severity: unrated
- source: `.result.missed[18]`
- anchors: `skills/debrief/SKILL.md:34`, `src/tools/transition-thread.mjs:56,59`

[skill-server-contract] skills/debrief/SKILL.md:34 — step 6 says to call transition_thread 'with the `thread_id` and the target status' and never names the parameter, while naming `blocked_by` at :36 and `closure_statement` at :37. The parameter is `to_status` (src/tools/transition-thread.mjs:56,59). Verified: {thread_id, status:'paused'} returns "(root) must have required property 'to_status'; (root) must NOT have additional properties" — recoverable, but it is the same unnamed-parameter pattern that makes step 5 unrecoverable.

### missed-20

- dimension: skill-server-contract
- severity: unrated
- source: `.result.missed[19]`
- anchors: `hooks/lib/stop.mjs:8`, `hooks/lib/stop.mjs:101-104`, `src/tools/transition-thread.mjs:40-41`, `skills/preflight/SKILL.md:4`, `skills/debrief/SKILL.md:4`

[skill-server-contract] hooks/lib/stop.mjs:8 — the block message is the only place the server ever re-surfaces a thread's ULID to the model after SessionStart, which would be the natural recovery path for a lost thread_id. It is unreachable in the documented resume flow: hooks/lib/stop.mjs:101-104 fires only when the active-thread pointer is set, and the only skill-reachable writer is src/tools/transition-thread.mjs:40-41, which requires to_status:'active' — a call neither skills/preflight/SKILL.md:4 nor skills/debrief/SKILL.md:4 permits.

### missed-21

- dimension: skill-server-contract
- severity: unrated
- source: `.result.missed[20]`
- anchors: `src/tools/record-decision.mjs:68-70`, `src/model/caps.mjs:9`, `src/model/caps.mjs:79-86`, `src/tools/update-thread.mjs:77`, `src/render/briefing.mjs:125`

[skill-server-contract] src/tools/record-decision.mjs:68-70 — record_decision appends {ref, title, scope} straight into spine.key_decisions and writes the thread at :76 without ever calling assertSpineCaps, and its own `title` schema (:91) has minLength 1 but no maxLength. src/model/caps.mjs:9 caps decisionTitleMaxChars at 120 and src/model/caps.mjs:79-86 enforces it, but only on what update_thread submits (src/tools/update-thread.mjs:77). So a 300-character decision title is written into the spine unchecked, is silently truncated at render (src/render/briefing.mjs:125), and then causes a CapViolationError if the model later restates that same decision through update_thread — a cap the creating tool never applied refusing the tool that restates it. Same cross-layer inconsistency class as the 200-char criterion cap already established.

### missed-22

- dimension: skill-server-contract
- severity: unrated
- source: `.result.missed[21]`
- anchors: `skills/preflight/SKILL.md:17`, `src/index/rebuild-index.mjs:44`, `hooks/lib/roster.mjs:33`

[skill-server-contract] skills/preflight/SKILL.md:17 — the skill tells the model that each roster entry 'carries its `thread_id`', but the token `thread_id` appears nowhere in the roster: src/index/rebuild-index.mjs:44 names the field `id`, and hooks/lib/roster.mjs:33 renders it as the trailing parenthetical `(id <ULID>)`. Minor, but it is the field the entire write side is keyed on and the skill teaches the wrong name for it.

### missed-23

- dimension: reliability
- severity: unrated
- source: `.result.missed[22]`
- anchors: `src/tools/record-decision.mjs:48`, `src/drivers/local-driver.mjs:147-153`, `src/tools/update-thread.mjs:105`, `src/tools/amend-criteria.mjs:116`, `src/tools/transition-thread.mjs:12`, `modelcontextprotocol/sdk/dist/cjs/shared/protocol.js:288`

[reliability] Lost update on concurrent read-modify-write of a thread record - no version, mtime or lock check anywhere on the write path. src/tools/record-decision.mjs:48 reads the thread and :76 writes it back; src/drivers/local-driver.mjs:147-153 writeThread validates and renames with no comparison against what is on disk. Reproduced: two record_decision calls issued with Promise.allSettled against one context both returned success, both decision files exist (decisions/0001-alpha.md, decisions/0001-beta.md), and the stored thread spine contains only {ref:'0001-alpha'} - the second writer clobbered the first's spine entry. Every mutating tool shares this shape (src/tools/update-thread.mjs:105/123, src/tools/amend-criteria.mjs:116, src/tools/transition-thread.mjs:12/39), and the MCP SDK dispatches requests concurrently (node_modules/@modelcontextprotocol/sdk/dist/cjs/shared/protocol.js:288).

### missed-24

- dimension: reliability
- severity: unrated
- source: `.result.missed[23]`
- anchors: `src/model/thread.mjs:53-59`, `src/tools/open-thread.mjs:17-18`, `src/tools/create-successor.mjs:15-20`, `src/index/rebuild-index.mjs:34-36`, `src/drift/reattach.mjs:42-47`, `src/drift/reattach.mjs:74-82`

[reliability] Thread slugs are never checked for uniqueness, and the by-slug index resolves a duplicate to the OLDEST thread. src/model/thread.mjs:53-59 derives the slug from the title with no collision check; src/tools/open-thread.mjs:17-18 and src/tools/create-successor.mjs:15-20 (which exposes no slug parameter at all) both write straight through. src/index/rebuild-index.mjs:34-36 keeps the first entry (`if (thread.slug && !(thread.slug in bySlug))`) over threads sorted by created_at (:5-15,27). src/drift/reattach.mjs:42-47 resolves a new branch through that index, so a successor sharing its terminal predecessor's title binds to the predecessor, and src/drift/reattach.mjs:74-82 then returns 'offer-successor' forever instead of binding the live thread.

### missed-25

- dimension: reliability
- severity: unrated
- source: `.result.missed[24]`
- anchors: `src/drivers/git-ref-driver.mjs:29`, `src/drivers/local-driver.mjs:252-255`, `src/drift/reattach.mjs:43-46`, `src/drift/reconcile.mjs:59`, `src/tools/reconcile.mjs:8`, `test/unit/drift/fake-driver.mjs:34-36`, `test/unit/drift/reattach.test.mjs:67`

[reliability] The by-slug index that branch reattachment depends on is empty in any freshly started process on the git backend, so the slug rung is dead in practice. index/ is gitignored (src/drivers/git-ref-driver.mjs:29, written :369) and the worktree is wiped on init (:356), which I observed leaving index/ empty after a second buildContext; src/drivers/local-driver.mjs:252-255 then returns {} and src/drift/reattach.mjs:43-46 yields `candidate === undefined`. src/drift/reconcile.mjs:59 calls reattach before src/tools/reconcile.mjs:8 ever rebuilds the index, so a process whose first action is reconcile can only ever match via the trailer or first-commit rungs. The slug rung is tested exclusively against an in-memory fake that pre-seeds the index (test/unit/drift/fake-driver.mjs:34-36, test/unit/drift/reattach.test.mjs:67).

### missed-26

- dimension: reliability
- severity: unrated
- source: `.result.missed[25]`
- anchors: `src/schema/thread.schema.mjs:17`, `src/schema/upcast.mjs:44,73`, `src/drivers/local-driver.mjs:148`, `src/drivers/local-driver.mjs:144`

[reliability] A v1 record containing an empty-string risk or out-of-scope entry upcasts into a record that fails v2 validation - a second instance of the read-fine/write-fail class, missed by the finder whose upcast analysis cited two mechanisms the v1 schema forbids. v1 declared spine.open_risks and spine.out_of_scope as `items: {type:'string'}` with no minLength (`git show bcc1861:src/schema/thread.schema.mjs`), while src/schema/thread.schema.mjs:17 (riskItem.text) and :106 (out_of_scope items) require minLength 1 and src/schema/upcast.mjs:44,73 copy the value straight through. Reproduced: both cases return invalid from validateThread after upcastThread, so src/drivers/local-driver.mjs:148 rejects a record src/drivers/local-driver.mjs:144 happily serves.

### missed-27

- dimension: reliability
- severity: unrated
- source: `.result.missed[26]`
- anchors: `hooks/lib/stop.mjs:67-73`, `hooks/lib/stop.mjs:39-42`

[reliability] The Stop hook's verbatim gate passes silently whenever the transcript yields no assistant text, including when it cannot be read at all. hooks/lib/stop.mjs:67-73 readAssistantTexts swallows every error into an empty array (and hooks/lib/stop.mjs:39-42 returns [] for a non-file or an over-size transcript), and :88-91 blocks only `if (texts.length > 0 && !echoed && !stopHookActive)`, otherwise falling through to clearPledge. So an unreadable or oversized transcript both clears the outstanding pledge and reports no violation - the gate fails open with no signal, on top of never firing at all on the git backend.

### missed-28

- dimension: security-and-injection
- severity: unrated
- source: `.result.missed[27]`
- anchors: `src/drivers/git-ref-driver.mjs:29`, `src/drivers/git-ref-driver.mjs:354-359`, `src/tools/get-resume-brief.mjs:30-34`, `hooks/lib/stop.mjs:59-65`, `bin/ledger-cli.mjs:51-57`, `hooks/lib/stop.mjs:83-93`, `src/tools/reconcile.mjs:7`, `hooks/lib/session-start.mjs:99-100`, `src/render/briefing.mjs:54-68`, `src/tools/get-resume-brief.mjs:28`, `test/unit/cli/briefing-pledge.test.mjs:7-13`

[security-and-injection] The verbatim gate is structurally dead under the git backend, and it takes the drift block with it. index/ is gitignored in the ledger worktree (src/drivers/git-ref-driver.mjs:29 and 367-370) so index files are never committed, and every context build rm -rf's the worktree before re-adding it from the ref (src/drivers/git-ref-driver.mjs:354-359). get_resume_brief writes the pledge to index/briefing.json (src/tools/get-resume-brief.mjs:30-34); the Stop hook reads it via the CLI (hooks/lib/stop.mjs:59-65 -> bin/ledger-cli.mjs:51-57), and that very CLI process wipes the file in buildContext BEFORE readIndexFile runs. I measured it: index dir before the CLI call = [briefing.json, by-branch.json, by-slug.json, children.json, resumable.json]; after = []. runCli(['briefing-pledge']) then returned null, so verbatimGate (hooks/lib/stop.mjs:83-93) can never fire in a git repo — the only control asserting the briefing was printed unaltered is inert. The same wipe destroys index/drift.json written by reconcile (src/tools/reconcile.mjs:7) before the roster CLI call later in the same SessionStart (hooks/lib/session-start.mjs:99-100) can be followed by any read, so renderBriefing's '## SINCE YOU LEFT' block (src/render/briefing.mjs:54-68, fed by takeDriftSnapshot at src/tools/get-resume-brief.mjs:28) is permanently empty. test/unit/cli/briefing-pledge.test.mjs:7-13 misses this because its fixture project is never git-inited (test/unit/cli/fixtures.mjs exports initGitRepo but project() never calls it), so it only exercises LocalDriver.

### missed-29

- dimension: security-and-injection
- severity: unrated
- source: `.result.missed[28]`
- anchors: `src/drivers/git-ref-driver.mjs:506-508`, `src/drift/classification.mjs:79-88`, `src/drift/disposition.mjs:38-48`, `src/drift/reconcile.mjs:43-46`, `src/tools/reconcile.mjs:8`, `src/model/binding.mjs:20`

[security-and-injection] reconcile silently rewrites other users' binding records from observations made against whatever repository happens to sit at binding.repo on THIS machine. If the path exists but the branch does not, observeBranch returns branch_exists:false (src/drivers/git-ref-driver.mjs:506-508 -> #observeDeleted, 536-556), classifyObservation emits branch-gone/'deleted' (src/drift/classification.mjs:79-88), disposeBinding returns binding_status 'orphaned' (src/drift/disposition.mjs:38-48), and runReconcile writes the closed binding with no confirmation (src/drift/reconcile.mjs:43-46), after which src/tools/reconcile.mjs:8 commits it and the next sync pushes it. Since repo is a machine-local absolute path shared through the ledger (src/model/binding.mjs:20), any teammate whose clone lives elsewhere — or whose machine merely has a same-named directory — permanently marks another user's active bindings orphaned in the shared record. This is the write-side twin of finding bind-branch-repo-unvalidated-cwd, which only covered the read-side crash.

### missed-30

- dimension: security-and-injection
- severity: unrated
- source: `.result.missed[29]`
- anchors: `src/drift/reconcile.mjs:53-63`, `src/drivers/git-ref-driver.mjs:572-579`, `src/drift/reattach.mjs:41-46`, `src/drift/slug.mjs:5`, `src/drift/reattach.mjs:75-88`

[security-and-injection] reconcile auto-creates bindings for branches it finds in an arbitrary repo path, matched by branch NAME. src/drift/reconcile.mjs:53-63 enumerates every branch of every bound repo via listRepoBranches (src/drivers/git-ref-driver.mjs:572-579) and calls reattach for each unbound one; src/drift/reattach.mjs:41-46 (trySlug) resolves through the by-slug index using branchSlug, which is only `branch.trim().replace(/\//g,'-')` (src/drift/slug.mjs:5), and on a hit writes a new binding (src/drift/reattach.mjs:75-88). A branch whose name slugifies to an existing thread slug therefore attaches itself to that thread with no human step, and the resulting record — including the foreign repo path and branch name — is committed and pushed.

### missed-31

- dimension: security-and-injection
- severity: unrated
- source: `.result.missed[30]`
- anchors: `src/render/briefing.mjs:62-65`, `src/drift/classification.mjs:100-108`, `src/tools/bind-branch.mjs:29`, `src/schema/binding.schema.mjs:23`, `src/drift/classification.mjs:65,74`

[security-and-injection] The drift block is the one briefing region with NO truncation at all and it renders attacker-controlled strings. src/render/briefing.mjs:62-65 interpolates entry.classification, entry.branch, signal.code and signal.detail raw into '- ...' lines with no truncate() call, unlike every other block. entry.branch is binding.branch (src/drift/classification.mjs:100-108), which bind_branch accepts as any non-empty string (src/tools/bind-branch.mjs:29) and the binding schema does not constrain (src/schema/binding.schema.mjs:23) — it is never round-tripped through git check-ref-format. signal.detail can also carry joined key-file lists (src/drift/classification.mjs:65,74). So a branch name containing newlines forges briefing sections exactly as the title does, with no length ceiling.

### missed-32

- dimension: security-and-injection
- severity: unrated
- source: `.result.missed[31]`
- anchors: `src/tools/read-decision.mjs:10-13`, `src/drivers/local-driver.mjs:215-224`, `skills/preflight/SKILL.md:26`

[security-and-injection] read_decision hands the model raw file content with no fencing and no provenance marker. src/tools/read-decision.mjs:10-13 returns `markdown` straight from src/drivers/local-driver.mjs:215-224 (a bare readFile), and skills/preflight/SKILL.md:26 instructs the model to call it whenever the user asks about a decision the briefing named. Decision files are authored by anyone with ledger write access — and, per the decision-number defect, orphaned decision files can be planted by a failing record_decision — so this is a second untrusted-content channel into the model with even less structure than the briefing.

### missed-33

- dimension: security-and-injection
- severity: unrated
- source: `.result.missed[32]`
- anchors: `hooks/lib/stop.mjs:87`, `skills/preflight/SKILL.md:24`

[security-and-injection] The verbatim gate, where it can run at all, is satisfied by substring containment: hooks/lib/stop.mjs:87 uses `texts.some((text) => text.includes(pledge.rendered))`. Any assistant message that quotes the briefing anywhere inside a larger response — including one that prefixes it with contradicting commentary or appends its own instructions — passes the check, so the gate cannot enforce the 'add nothing before or after it' half of skills/preflight/SKILL.md:24 that it exists to enforce.

## Supplement — MSP-0B coverage gap

This section is GENERATED from `docs/audits/2026-08-04-mcp-audit-supplement.json`, a separate artifact. The original
`docs/audits/2026-08-04-mcp-audit.json` is byte-frozen and was NOT modified: its sha256 is recorded
in the SPEC as proof that the committed copy is byte-identical to the workflow output, and
re-serializing it would destroy that provenance.

The first audit covered five dimensions, but three areas were only grepped, never read: the `src/drift/*` internals, `src/schema/upcast.mjs` and the full v1 to v2 path, and every `GitRefDriver` payload measurement. Absence of findings there was not evidence of absence. Two of the twelve criticals already lived in `git-ref-driver.mjs` and were found incidentally rather than systematically. MSP-7, MSP-9 and MSP-11 are each specified from partial knowledge of exactly these areas, and this supplement gates them.

Supplement ids all carry a `sup-` prefix, so they can never collide with the ids above. Everything
before this heading is the original 121 defects and is unchanged by the presence of this section.

### Supplement counts

| Population | JSON path | Count |
| --- | --- | --- |
| Supplement findings | `.result.findings` | 29 |
| Files read in full | `.result.coverage.files` | 33 |

- severity: critical 6, high 8, medium 11, low 4
- verdict: CONFIRMED 20, NARROWED 8, REFUTED 1
- area: drift 9, git-ledger 1, git-ref 10, upcast 9

### Coverage

| file | lines | read |
| --- | --- | --- |
| `src/drift/classification.mjs` | 102 | full |
| `src/drift/disposition.mjs` | 58 | full |
| `src/drift/reattach.mjs` | 103 | full |
| `src/drift/reconcile.mjs` | 67 | full |
| `src/drift/slug.mjs` | 6 | full |
| `src/drift/snapshot.mjs` | 93 | full |
| `src/drift/index.mjs` | 12 | full |
| `src/schema/upcast.mjs` | 89 | full |
| `src/schema/thread.schema.mjs` | 112 | full |
| `src/schema/patterns.mjs` | 24 | full |
| `src/drivers/git-ref-driver.mjs` | 580 | full |
| `src/drivers/git-ledger.mjs` | 83 | full |
| `src/drivers/select.mjs` | 49 | full |
| `src/drivers/layout.mjs` | 3 | full |
| `src/drivers/local-driver.mjs` | 306 | full |
| `src/drivers/storage-driver.mjs` | 89 | full |
| `src/util/git-scope.mjs` | 102 | full |
| `src/util/active-thread.mjs` | 59 | full |
| `src/util/ulid.mjs` | 11 | full |
| `src/model/selection.mjs` | 49 | full |
| `src/model/binding.mjs` | 29 | full |
| `src/index/rebuild-index.mjs` | 77 | full |
| `src/index/index-files.mjs` | 5 | full |
| `src/tools/reconcile.mjs` | 17 | full |
| `src/tools/get-resume-brief.mjs` | 50 | full |
| `src/tools/update-thread.mjs` | 154 | full |
| `src/tools/rebuild-index.mjs` | 15 | full |
| `src/tools/shared.mjs` | 23 | full |
| `src/tools/context.mjs` | 12 | full |
| `hooks/lib/cli.mjs` | 39 | full |
| `hooks/lib/session-start.mjs` | 104 | full |
| `bin/ledger-server.mjs` | 86 | full |
| `bin/ledger-cli.mjs` | 234 | read for the commands the drift and index paths reach: roster, reconcile, briefing-pledge, active-thread, sync (lines 1-90, 190-234) |

Not read, and therefore not covered by this supplement:

- src/render/briefing.mjs — MSP-10's surface, out of this MSP's scope; read only where the drift block's consumption of `src/drift/*` output had to be confirmed.
- src/tools/* beyond the five listed above — covered by the first audit's five dimensions.
- test/** — tests are not evidence of behavior under this SPEC's evidence law; two test files are cited only where an amendment requires changing them.

### Supplement findings

| id | area | severity | verdict | title | anchor |
| --- | --- | --- | --- | --- | --- |
| `sup-gitref-init-destroys-every-index-file` | git-ref | critical | CONFIRMED | GitRefDriver.init() rm -rf's the worktree on every process start, and because index/ is gitignored and lives inside it, every derived index file is destroyed with nothing to rebuild it from | `src/drivers/git-ref-driver.mjs:356` |
| `sup-gitref-sessionstart-destroys-its-own-drift` | git-ref | critical | CONFIRMED | The SessionStart hook's own three-process sequence destroys the drift snapshot it just wrote, so the entire src/drift feature is non-functional end to end on the deployed backend | `hooks/lib/session-start.mjs:98-100` |
| `sup-gitref-verbatim-gate-can-never-fire` | git-ref | critical | CONFIRMED | The Stop hook's verbatim gate reads its pledge through a separate CLI process whose own init() has already deleted it, so the gate returns null and never fires on the git backend | `src/tools/get-resume-brief.mjs:30-34` |
| `sup-gitref-second-process-destroys-uncommitted-writes` | git-ref | critical | CONFIRMED | Starting a second server or CLI process destroys the first process's uncommitted thread, binding and decision writes outright | `src/drivers/git-ref-driver.mjs:356` |
| `sup-gitref-worktree-lock-gates-nothing` | git-ref | high | CONFIRMED | The worktree lock never prevents the destructive path: a failed acquisition still runs #provisionWorktree, and `locked` is consulted only to decide whether to release | `src/drivers/git-ref-driver.mjs:346-351` |
| `sup-gitref-commit-finally-clears-the-only-guard` | git-ref | medium | CONFIRMED | commit() clears the uncommitted-write flag in a finally block, so a commit that throws leaves real uncommitted writes with the guard that protects them switched off | `src/drivers/git-ref-driver.mjs:399-403` |
| `sup-gitref-reads-never-repair-and-report-empty` | git-ref | high | CONFIRMED | Only the write path can repair a missing worktree; every read path returns null or an empty array instead, so a destroyed ledger reads as an empty one rather than as an error | `src/drivers/git-ref-driver.mjs:282-287` |
| `sup-gitref-merge-theirs-is-the-sole-discard-not-the-push` | git-ref | critical | NARROWED | The silent discard in ledger sync comes entirely from `-X theirs`; the push is leased, so the SPEC's framing of critical 3 as a force-push problem points at the wrong half | `src/drivers/git-ref-driver.mjs:490` |
| `sup-gitref-upstream-signals-silently-neutral` | git-ref | medium | NARROWED | Both upstream drift WARNINGs return a neutral value instead of "unknown" whenever refs/remotes/origin/<branch> does not resolve, which includes every branch that has never been pushed | `src/drivers/git-ref-driver.mjs:106` |
| `sup-gitref-first-commit-falls-back-to-repo-root` | git-ref | critical | CONFIRMED | firstCommitOf falls back to the repository root commit, so in a repo with no origin remote EVERY branch reports the same first_commit and a single binding silently captures every unbound branch, main included | `src/drivers/git-ref-driver.mjs:146-151` |
| `sup-drift-reconcile-aborts-on-an-absent-bound-repo` | drift | high | CONFIRMED | One binding naming a repository path absent from this machine aborts the entire reconcile with `spawn git ENOENT`, discarding all drift computed before it, and the SessionStart hook swallows the failure entirely | `src/drift/reconcile.mjs:35` |
| `sup-drift-closed-binding-blocks-reattachment-forever` | drift | medium | CONFIRMED | A closed binding keeps reserving its repo/branch key, so a branch that is deleted and later recreated can never be re-attached: the drift loop skips the closed binding and the reattach loop skips the branch | `src/drift/reconcile.mjs:32-34` |
| `sup-drift-disposition-invents-a-recommendation-for-a-missing-thread` | drift | medium | CONFIRMED | disposeBinding treats a thread that does not exist as non-terminal, so a binding pointing at a deleted thread yields a `complete` recommendation and is silently closed | `src/drift/reconcile.mjs:41-42` |
| `sup-drift-resolved-drift-is-never-pruned` | drift | medium | CONFIRMED | The drift snapshot only ever grows: an entry whose drift has since been resolved is carried forward indefinitely, because the merge replaces matching keys and drops nothing | `src/drift/snapshot.mjs:37-40` |
| `sup-drift-three-of-eight-signal-codes-are-unreachable` | drift | medium | CONFIRMED | The only production observation source hardcodes force_push_detected false and both key-file arrays empty, so three of the eight drift signal codes — two of them CRITICAL — can never fire | `src/drivers/git-ref-driver.mjs:529-532` |
| `sup-drift-terminal-reattach-reports-a-match-it-did-not-make` | drift | low | CONFIRMED | reattach returns matched:true with a null binding for a terminal thread, and runReconcile records it as a reattach disposition, so the caller is told a branch was attached when no binding was written | `src/drift/reattach.mjs:74-82` |
| `sup-drift-thread-id-trailer-is-an-unauthenticated-attach-primitive` | drift | medium | NARROWED | Any commit message trailer named Thread-Id attaches its branch to that thread with no further check, and the binding is written without user action | `src/drivers/git-ref-driver.mjs:162` |
| `sup-drift-bindings-store-machine-local-absolute-paths` | drift | medium | CONFIRMED | Bindings persist an absolute repository path, and every drift operation re-derives observations by running git at that path on whatever machine reads the ledger | `src/model/binding.mjs:19` |
| `sup-drift-snapshot-read-throws-on-a-malformed-index` | drift | low | NARROWED | takeDriftSnapshot asserts the stored snapshot's shape and throws, so a single malformed drift index makes get_resume_brief fail outright rather than degrade | `src/drift/snapshot.mjs:85` |
| `sup-upcast-decision-ref-copied-verbatim` | upcast | high | CONFIRMED | The v1 upcast copies a legacy decision string verbatim into `ref` while the split regex gates only the title, so any non-conforming v1 decision produces a thread that reads but can never be written again | `src/schema/upcast.mjs:55` |
| `sup-upcast-manufactures-an-empty-decision-title` | upcast | medium | NARROWED | unkebab returns its input unchanged when the de-kebabbed string is empty, so an empty-string v1 decision upcasts to a title that violates minLength | `src/schema/upcast.mjs:33-35` |
| `sup-upcast-passes-unknown-keys-through-a-closed-schema` | upcast | high | CONFIRMED | The upcast spreads both the record and the spine wholesale into a schema that forbids additional properties, and supplies no default for the two required spine scalars | `src/schema/upcast.mjs:67-69` |
| `sup-upcast-drops-criteria-and-copies-text-unguarded` | upcast | high | CONFIRMED | A non-array v1 completion_criteria upcasts to an empty array — a valid record with the criteria permanently gone — and a criterion missing text upcasts to a record that cannot be written | `src/schema/upcast.mjs:28` |
| `sup-upcast-returns-unknown-versions-raw-and-unvalidated` | upcast | medium | NARROWED | upcastThread returns the record untouched and unvalidated for any schema_version that is not the number 1, so absent, 0, 3 and the string "1" all reach every tool as raw v1-shaped data | `src/schema/upcast.mjs:82` |
| `sup-upcast-destroys-criterion-identity-on-every-read` | upcast | high | NARROWED | The upcast renumbers criterion ids positionally and hardcodes kind and struck_by, so one read-modify-write silently breaks the scope references that point at criterion ids and un-strikes struck criteria | `src/schema/upcast.mjs:19` |
| `sup-upcast-legacy-scope-can-never-be-removed` | upcast | medium | CONFIRMED | The upcast stamps scope "legacy" on every migrated decision, and every write path refuses that scope, so upcast decisions are immortal and permanently constrain out_of_scope | `src/schema/upcast.mjs:57` |
| `sup-upcast-caps-consolidation-invalidates-what-the-upcast-produces` | upcast | high | CONFIRMED | MSP-7's own plan to move SPINE_CAPS into the schema will newly invalidate seven classes of value the upcast manufactures today, and two existing tests assert the current permissive behavior | `src/schema/upcast.mjs:56` |
| `sup-upcast-listthreads-divergence-is-narrower-than-assumed` | upcast | low | NARROWED | listThreads not upcasting is real, but for a well-formed v1 record the roster entry is byte-identical either way; the genuine hazard is a bare-string criterion, where the list succeeds and the upcast throws | `src/drivers/local-driver.mjs:160` |
| `sup-refuted-ledger-commit-env-cannot-clear-inherited-dates` | git-ledger | low | REFUTED | REFUTED — the undefined values in ledgerCommitEnv do not fail to clear an inherited GIT_AUTHOR_DATE | `src/drivers/git-ledger.mjs:19` |

### Measurements

Every payload figure in the SPEC was measured on `LocalDriver`; the deployed backend is
`GitRefDriver`. Both figures are recorded here. Measured against temp ledgers, never a real one.

| figure | LocalDriver | GitRefDriver | note |
| --- | --- | --- | --- |
| update_thread tool result, JSON.stringify bytes | 6,514 B | 6,514 B | SPEC's 6,269 B verified in magnitude; the 4% delta is the reconstructed thread, not the backend. Driver-independent: src/tools/update-thread.mjs:125 returns the record. |
| update_thread as it reaches the model, MCP content envelope | 6,963 B | 6,963 B | bin/ledger-server.mjs:52 — `{ content: [{ type: 'text', text: JSON.stringify(result) }] }`. This, not the raw result, is the figure MSP-6's under-1,200-byte target must beat. |
| update_thread section breakdown | criteria 1,140 / risks 1,953 / decisions 1,007 / out_of_scope 1,321 | identical | 5,421 B of 6,514 (83%) is content the caller submitted earlier and did not touch, matching the original audit's 83%. |
| roster / thread listing, 17 threads | 11,519 B | 11,519 B | `list_threads` DOES NOT EXIST — MSP-5 introduces it. Measured the closest existing surface: the `resumable` index array that rebuild-index.mjs:45-54 builds and bin/ledger-cli.mjs:22-26 returns. 17 threads matches the real .windful-ocean ledger size the SPEC cites in I2. |
| roster / thread listing, 1 thread | 248 B | 248 B | ~677 B per entry at scale, dominated by next_step, whose cap is 500 chars (caps.mjs:3). MSP-12's pagination target should assume this per-entry cost. |
| index rebuild, 17 threads | 1.8 ms | 2.0 ms | rebuildIndex alone (src/index/rebuild-index.mjs:17). |
| index rebuild, 1 thread | 1.1 ms | 1.0 ms | Rebuild cost is near-flat from 1 to 17 threads. |
| commitAndReindex, 17 threads — what every mutating tool actually pays | 30.1 ms | 27.3 ms | src/tools/shared.mjs:19-23. The rebuild is ~7% of this; the git commit is the rest. Both backends pay it, because LocalDriver also drives a git recovery repo (local-driver.mjs:278-301). MSP-12's premise that the per-call index rebuild is the cost driver is not supported by this measurement. |
| commitAndReindex, 1 thread | 26.9 ms | 27.6 ms | Near-flat, confirming the cost is per-commit, not per-thread, at this scale. |
| index files surviving one further process start | 3 of 3 survive | 0 of 3 survive | drift snapshot, briefing pledge and by-slug map. THIS is where the backends actually diverge, and it is a durability difference, not a size one. See sup-gitref-init-destroys-every-index-file. |

Harness: `scratchpad/measure/measure.mjs` and `scratchpad/measure/durability.mjs`, run against throwaway ledgers created under a temp CLAUDE_PLUGIN_DATA with a temp git repository. No real ledger was read or written. The thread used for the payload figures reproduces the shape the original audit describes for finding `write-tools-echo-whole-thread`: 8 criteria, 6 risks with refs, 9 decisions, 12 out_of_scope entries and capped-length scalars. Timings are the median of 5 runs.
