diff --git a/docs/registers/size-limits.json b/docs/registers/size-limits.json
index b1999edc..11533dbc 100644
--- a/docs/registers/size-limits.json
+++ b/docs/registers/size-limits.json
@@ -82,7 +82,7 @@
   },
   {
     "name": "BRIEFING_MAX_CHARS",
-    "site": "src/render/briefing.ts:36",
+    "site": "src/render/briefing.ts:31",
     "value": 12000,
     "basis": "chosen",
     "reason": "Measured build-up of the worst case reaches about 10,600 characters and 12,000 is the smallest round ceiling with headroom (docs/specs/2026-08-26-briefing-scoping-repair.md:237-246,290-292); human-ruled, against the architect measurement that the prior spec 3,000 ceiling is unreachable at today caps. This ceiling is the target the clip search aims at, not a bound the reply always respects: a briefing that cannot fit even with every field rendered at its guaranteed minimum length is returned over this ceiling, and the page says so.",
@@ -91,7 +91,7 @@
   },
   {
     "name": "RESUME_PAYLOAD_MAX_BYTES",
-    "site": "src/render/briefing.ts:37",
+    "site": "src/render/briefing.ts:32",
     "value": 12418,
     "basis": "derived",
     "reason": "The reply carries the briefing once, so this is BRIEFING_MAX_CHARS plus RESUME_PAYLOAD_RESERVE_BYTES plus the measured envelope of 26 thread-id bytes, 114 scaffold bytes and 78 previous-session bytes. It is the smallest ceiling at which a briefing sitting exactly on the character target always serialises, so bytes never bind plain ASCII and bind only content whose byte length exceeds its character length. This ceiling is the target the clip search aims at, not a bound the reply always respects: a briefing that cannot fit even with every field rendered at its guaranteed minimum length is returned over this ceiling, and the page says so.",
@@ -100,7 +100,7 @@
   },
   {
     "name": "RESUME_PAYLOAD_RESERVE_BYTES",
-    "site": "src/render/briefing.ts:39",
+    "site": "src/render/briefing.ts:34",
     "value": 200,
     "basis": "chosen",
     "reason": "Target 12,218 bytes rather than 12,418 because the convergent search fills to whatever byte budget it is given, so budgeting at the raw cap would land content exactly on it, and a knife-edge equality is fragile against any later change; test/unit/briefing.test.ts:974 (briefing.the-clip-search-lands-just-under-the-byte-cap-on-the-worst-reachable-multi-byte-record) exercises the search converging against this target.",
@@ -109,7 +109,7 @@
   },
   {
     "name": "RESUME_PAYLOAD_TARGET_BYTES",
-    "site": "src/render/briefing.ts:40",
+    "site": "src/render/briefing.ts:35",
     "value": 12218,
     "basis": "unrecorded",
     "reason": null,
@@ -118,7 +118,7 @@
   },
   {
     "name": "BRIEFING_COPIES_IN_RESUME_PAYLOAD",
-    "site": "src/render/briefing.ts:42",
+    "site": "src/render/briefing.ts:37",
     "value": 1,
     "basis": "derived",
     "reason": "The reply carries the briefing exactly once, in structuredContent.briefing; the text slot the resume_thread tool returns carries no copy of it.",
@@ -127,7 +127,7 @@
   },
   {
     "name": "RESUME_PAYLOAD_SCAFFOLD_BYTES",
-    "site": "src/render/briefing.ts:43",
+    "site": "src/render/briefing.ts:38",
     "value": 114,
     "basis": "unrecorded",
     "reason": null,
@@ -136,7 +136,7 @@
   },
   {
     "name": "PREVIOUS_SESSION_NULL_BYTES",
-    "site": "src/render/briefing.ts:44",
+    "site": "src/render/briefing.ts:39",
     "value": 4,
     "basis": "unrecorded",
     "reason": null,
@@ -145,7 +145,7 @@
   },
   {
     "name": "PREVIOUS_SESSION_LARGEST_BYTES",
-    "site": "src/render/briefing.ts:45",
+    "site": "src/render/briefing.ts:40",
     "value": 82,
     "basis": "unrecorded",
     "reason": null,
@@ -154,7 +154,7 @@
   },
   {
     "name": "PREVIOUS_SESSION_PRESENT_EXTRA_BYTES",
-    "site": "src/render/briefing.ts:46",
+    "site": "src/render/briefing.ts:41",
     "value": 78,
     "basis": "unrecorded",
     "reason": null,
@@ -163,7 +163,7 @@
   },
   {
     "name": "PREVIOUS_SESSION_ABSENT_EXTRA_BYTES",
-    "site": "src/render/briefing.ts:47",
+    "site": "src/render/briefing.ts:42",
     "value": 0,
     "basis": "unrecorded",
     "reason": null,
@@ -172,7 +172,7 @@
   },
   {
     "name": "JSON_STRING_DELIMITER_BYTES",
-    "site": "src/render/briefing.ts:49",
+    "site": "src/render/briefing.ts:44",
     "value": 2,
     "basis": "unrecorded",
     "reason": null,
@@ -181,7 +181,7 @@
   },
   {
     "name": "FORMER_RISK_TEXT_MAX",
-    "site": "src/render/briefing.ts:26",
+    "site": "src/render/briefing.ts:20",
     "value": 500,
     "basis": "unrecorded",
     "reason": null,
@@ -190,7 +190,16 @@
   },
   {
     "name": "FORMER_CRITERION_SETTLED_BY_MAX",
-    "site": "src/render/briefing.ts:27",
+    "site": "src/render/briefing.ts:21",
+    "value": 500,
+    "basis": "unrecorded",
+    "reason": null,
+    "mirrors": null,
+    "mirror_relation": null
+  },
+  {
+    "name": "FORMER_HEADER_FIELD_WRITE_MAX",
+    "site": "src/render/briefing.ts:22",
     "value": 500,
     "basis": "unrecorded",
     "reason": null,
@@ -199,79 +208,79 @@
   },
   {
     "name": "RELATED_TITLE_FLOOR",
-    "site": "src/render/briefing.ts:68",
+    "site": "src/render/briefing.ts:63",
     "value": 100,
     "basis": "unrecorded",
     "reason": null,
-    "mirrors": "src/schema/caps.ts:1",
-    "mirror_relation": "at-most"
+    "mirrors": null,
+    "mirror_relation": null
   },
   {
     "name": "RELATED_SLUG_FLOOR",
-    "site": "src/render/briefing.ts:69",
+    "site": "src/render/briefing.ts:64",
     "value": 64,
     "basis": "derived",
     "reason": "Equals THREAD_SLUG_MAX, the write cap for this field, so a value stored at its maximum renders whole.",
-    "mirrors": "src/schema/caps.ts:2",
+    "mirrors": "src/schema/caps.ts:1",
     "mirror_relation": "equal"
   },
   {
     "name": "RISK_TEXT_FLOOR",
-    "site": "src/render/briefing.ts:70",
+    "site": "src/render/briefing.ts:65",
     "value": 500,
     "basis": "derived",
     "reason": "Equals FORMER_RISK_TEXT_MAX, the last value of RISK_TEXT_MAX before it was removed as a write cap that did no work, so a value stored at that length still renders whole.",
-    "mirrors": "src/render/briefing.ts:26",
+    "mirrors": "src/render/briefing.ts:20",
     "mirror_relation": "equal"
   },
   {
     "name": "RISK_REF_FLOOR",
-    "site": "src/render/briefing.ts:71",
+    "site": "src/render/briefing.ts:66",
     "value": 200,
     "basis": "derived",
     "reason": "Equals RISK_REF_MAX, the write cap for this field, so a value stored at its maximum renders whole.",
-    "mirrors": "src/schema/caps.ts:21",
+    "mirrors": "src/schema/caps.ts:14",
     "mirror_relation": "equal"
   },
   {
     "name": "KEY_DECISION_TITLE_FLOOR",
-    "site": "src/render/briefing.ts:72",
+    "site": "src/render/briefing.ts:67",
     "value": 200,
     "basis": "derived",
     "reason": "Equals KEY_DECISION_TITLE_MAX, the write cap for this field, so a value stored at its maximum renders whole.",
-    "mirrors": "src/schema/caps.ts:24",
+    "mirrors": "src/schema/caps.ts:17",
     "mirror_relation": "equal"
   },
   {
     "name": "OUT_OF_SCOPE_TEXT_FLOOR",
-    "site": "src/render/briefing.ts:73",
+    "site": "src/render/briefing.ts:68",
     "value": 300,
     "basis": "derived",
     "reason": "Equals OUT_OF_SCOPE_TEXT_MAX, the write cap for this field, so a value stored at its maximum renders whole.",
-    "mirrors": "src/schema/caps.ts:28",
+    "mirrors": "src/schema/caps.ts:21",
     "mirror_relation": "equal"
   },
   {
     "name": "CRITERION_TEXT_FLOOR",
-    "site": "src/render/briefing.ts:74",
+    "site": "src/render/briefing.ts:69",
     "value": 500,
     "basis": "derived",
     "reason": "Equals CRITERION_TEXT_MAX, the write cap for this field, so a value stored at its maximum renders whole.",
-    "mirrors": "src/schema/caps.ts:15",
+    "mirrors": "src/schema/caps.ts:8",
     "mirror_relation": "equal"
   },
   {
     "name": "CRITERION_CHECK_FLOOR",
-    "site": "src/render/briefing.ts:75",
+    "site": "src/render/briefing.ts:70",
     "value": 500,
     "basis": "derived",
     "reason": "Equals CRITERION_CHECK_MAX, the write cap for this field, so a value stored at its maximum renders whole.",
-    "mirrors": "src/schema/caps.ts:16",
+    "mirrors": "src/schema/caps.ts:9",
     "mirror_relation": "equal"
   },
   {
     "name": "CRITERION_RESULT_FLOOR",
-    "site": "src/render/briefing.ts:76",
+    "site": "src/render/briefing.ts:71",
     "value": 500,
     "basis": "unrecorded",
     "reason": null,
@@ -280,52 +289,52 @@
   },
   {
     "name": "CRITERION_SETTLED_BY_FLOOR",
-    "site": "src/render/briefing.ts:77",
+    "site": "src/render/briefing.ts:72",
     "value": 500,
     "basis": "derived",
     "reason": "Equals FORMER_CRITERION_SETTLED_BY_MAX, the last value of CRITERION_SETTLED_BY_MAX before it was removed as a write cap that did no work, so a value stored at that length still renders whole.",
-    "mirrors": "src/render/briefing.ts:27",
+    "mirrors": "src/render/briefing.ts:21",
     "mirror_relation": "equal"
   },
   {
     "name": "SESSION_ENTRY_TEXT_FLOOR",
-    "site": "src/render/briefing.ts:78",
+    "site": "src/render/briefing.ts:73",
     "value": 200,
     "basis": "chosen",
     "reason": "The guaranteed minimum for both shapes the session log renders: an older entry's first stored line, and the newest entry's block once it alone is too large to render whole. Chosen, not derived. It equals SESSION_FIRST_LINE_MAX at src/server/resource-render.ts:22, the limit the sessions resource already applies to the same quantity, so the two surfaces guarantee the same amount of a first line; that constant's own reason, 50 entries times 200 graphemes under the 12,000 briefing budget, does NOT carry over, because the briefing has no item cap and 145 entries are reachable. Measured on 2026-09-10 over the 145 entries of thread 01M24FHCMWGH0SHARH12CARCZ1: the escaped first line runs a median of 141 graphemes and 63.4 percent sit at or below 200. 500, the placeholder this replaces, leaves a projected 47,167-character page where 200 leaves 36,650; 80 sits below the median and cuts four headlines in five mid-clause. No value makes that thread fit, so none was chosen to try: the residue there is entry count, which decision B16 forbids addressing with a display-time item cap.",
-    "mirrors": "src/schema/caps.ts:41",
+    "mirrors": "src/schema/caps.ts:34",
     "mirror_relation": "at-most"
   },
   {
     "name": "ARTIFACT_LABEL_FLOOR",
-    "site": "src/render/briefing.ts:79",
+    "site": "src/render/briefing.ts:74",
     "value": 200,
     "basis": "derived",
     "reason": "Equals ARTIFACT_LABEL_MAX, the write cap for this field, so a value stored at its maximum renders whole.",
-    "mirrors": "src/schema/caps.ts:30",
+    "mirrors": "src/schema/caps.ts:23",
     "mirror_relation": "equal"
   },
   {
     "name": "ARTIFACT_POINTER_FLOOR",
-    "site": "src/render/briefing.ts:80",
+    "site": "src/render/briefing.ts:75",
     "value": 500,
     "basis": "derived",
     "reason": "Equals ARTIFACT_POINTER_MAX, the write cap for this field, so a value stored at its maximum renders whole.",
-    "mirrors": "src/schema/caps.ts:31",
+    "mirrors": "src/schema/caps.ts:24",
     "mirror_relation": "equal"
   },
   {
     "name": "HEADER_FIELD_ESCAPED_GRAPHEME_MAX",
-    "site": "src/render/briefing.ts:82",
+    "site": "src/render/briefing.ts:77",
     "value": 500,
     "basis": "derived",
-    "reason": "The largest stored write cap among the six briefing header fields is 500: SPINE_ACTIVE_GOAL_MAX, SPINE_NEXT_STEP_MAX, SPINE_LAST_SESSION_MAX, SPINE_LANDED_MAX and THREAD_BLOCKED_BY_MAX in src/schema/caps.ts, while THREAD_TITLE_MAX is 200. This ceiling is measured after escaping, so what it guarantees is narrower than that write cap: a stored value whose escape is the identity renders whole even when it already sits at its write cap, and any value the escape expands past 500 is shortened here by design. The escape is not the identity on every plain-ASCII value, because escapeStored expands a markdown leading character at line start and always expands '<'; an all-ASCII 500-character value opening '- fix the parser' or containing 'a < b' measures 505 escaped and is shortened. No finite budget could avoid that, since 500 stored '<' characters escape to 3000 characters. The cap is therefore a different quantity from any one write cap rather than a copy of one, and it binds only once escaping expands a value past the largest of them.",
-    "mirrors": "src/schema/caps.ts:8",
-    "mirror_relation": "at-least"
+    "reason": "Equals FORMER_HEADER_FIELD_WRITE_MAX, 500, the largest write cap the six briefing header fields carried before those caps were removed on 2026-09-14 (decision 01M2EDZAYDQZM9H42WEWKPMC9T). The clip is kept at that value because the briefing render budget is out of scope until the second pass (decision 01M2GKAHSZ2FFS9VE0R9ZV96D3). It is measured after escaping and applies to the title, blocked_by, active_goal, landed and a legacy last_session; the next step renders whole.",
+    "mirrors": "src/render/briefing.ts:22",
+    "mirror_relation": "equal"
   },
   {
     "name": "MIN_TEXT_CLIP",
-    "site": "src/render/briefing.ts:91",
+    "site": "src/render/briefing.ts:79",
     "value": 14,
     "basis": "unrecorded",
     "reason": null,
@@ -334,7 +343,7 @@
   },
   {
     "name": "NO_CLIP",
-    "site": "src/render/briefing.ts:92",
+    "site": "src/render/briefing.ts:80",
     "value": "Infinity",
     "basis": "unrecorded",
     "reason": null,
@@ -343,29 +352,29 @@
   },
   {
     "name": "CLIP_SEARCH_UPPER_BOUND",
-    "site": "src/render/briefing.ts:287",
+    "site": "src/render/briefing.ts:275",
     "value": 32000,
     "basis": "derived",
     "reason": "The largest stored text any briefing field can hold, so no clip the search sets is ever capped below a value the store admits. It is no longer the top of the search itself: the search runs to CLIP_SEARCH_AXIS_UPPER_BOUND, which composes this ceiling with the per-item clip span. The thirteen per-field limits do not bound it, because a floor is a minimum and cannot bound a search from above.",
-    "mirrors": "src/schema/caps.ts:41",
+    "mirrors": "src/schema/caps.ts:34",
     "mirror_relation": "equal"
   },
   {
     "name": "CLIP_SEARCH_AXIS_LOWER_BOUND",
-    "site": "src/render/briefing.ts:288",
+    "site": "src/render/briefing.ts:276",
     "value": 200,
     "basis": "derived",
     "reason": "Equals SESSION_ENTRY_TEXT_FLOOR. The bottom of the clip search is every field at its floor, and the newest session entry is never shortened below what an older entry's headline is guaranteed, so the axis bottoms out at that floor rather than at MIN_TEXT_CLIP. The threshold below which clipWithMarker returns the shortening marker alone is CLIP_MARKER_GRAPHEMES, which is 14 and not this floor (src/render/clip.ts:22-23); from 15 upward it returns real text with the marker appended. What the floor of 200 buys above that threshold is a headline that keeps 186 graphemes of its own text, the same amount of a first line the sessions resource already shows (src/server/resource-render.ts:22), rather than the handful of graphemes a bottom near the marker threshold would leave.",
-    "mirrors": "src/render/briefing.ts:78",
+    "mirrors": "src/render/briefing.ts:73",
     "mirror_relation": "equal"
   },
   {
     "name": "CLIP_SEARCH_AXIS_UPPER_BOUND",
-    "site": "src/render/briefing.ts:289",
+    "site": "src/render/briefing.ts:277",
     "value": 63986,
     "basis": "derived",
     "reason": "The search runs one axis that composes two ranges so a single binary search expresses the render priority. At or below CLIP_SEARCH_UPPER_BOUND the axis point is the newest session entry's clip while every other field sits at its floor; above it the newest renders whole and the axis point drives every other field's clip from MIN_TEXT_CLIP up to CLIP_SEARCH_UPPER_BOUND. The top is therefore CLIP_SEARCH_UPPER_BOUND plus the per-item clip span, 32000 + (32000 - 14). Page size does decrease at one point on the axis: where the last shortened value stops being shortened, TEXT_CLIPPED_BULLET goes with the Not shown heading and the See logbook://thread/ line, about 210 characters against a step of about 1. That dip skips no fitting page. An axis point at which nothing is clipped renders byte-identically to the unclipped render, which renderBriefingWithPasses tries and rejects before it ever enters the search (src/render/briefing.ts:585-586), so the dip lands on a page already known not to fit. The pass ceiling is a consequence of halving a fixed range rather than of any monotonicity: bisection over a range of this size cannot run longer whatever shape fits() has, so the ceiling keeps its expression 1 + ceil(log2(range)) at test/unit/briefing.test.ts:846-847 and evaluates to 17 on this range, one render more than the 16 it evaluated to before.",
-    "mirrors": "src/render/briefing.ts:287",
+    "mirrors": "src/render/briefing.ts:275",
     "mirror_relation": "at-least"
   },
   {
@@ -440,36 +449,18 @@
     "mirrors": null,
     "mirror_relation": null
   },
-  {
-    "name": "THREAD_TITLE_MAX",
-    "site": "src/schema/caps.ts:1",
-    "value": 200,
-    "basis": "unrecorded",
-    "reason": null,
-    "mirrors": null,
-    "mirror_relation": null
-  },
   {
     "name": "THREAD_SLUG_MAX",
-    "site": "src/schema/caps.ts:2",
+    "site": "src/schema/caps.ts:1",
     "value": 64,
     "basis": "unrecorded",
     "reason": null,
     "mirrors": null,
     "mirror_relation": null
   },
-  {
-    "name": "THREAD_BLOCKED_BY_MAX",
-    "site": "src/schema/caps.ts:3",
-    "value": 500,
-    "basis": "unrecorded",
-    "reason": null,
-    "mirrors": null,
-    "mirror_relation": null
-  },
   {
     "name": "THREAD_CLOSURE_DETAIL_MAX",
-    "site": "src/schema/caps.ts:4",
+    "site": "src/schema/caps.ts:2",
     "value": 2000,
     "basis": "unrecorded",
     "reason": null,
@@ -478,52 +469,16 @@
   },
   {
     "name": "BINDING_BRANCH_MAX",
-    "site": "src/schema/caps.ts:6",
+    "site": "src/schema/caps.ts:4",
     "value": 255,
     "basis": "unrecorded",
     "reason": null,
     "mirrors": null,
     "mirror_relation": null
   },
-  {
-    "name": "SPINE_ACTIVE_GOAL_MAX",
-    "site": "src/schema/caps.ts:8",
-    "value": 500,
-    "basis": "unrecorded",
-    "reason": null,
-    "mirrors": null,
-    "mirror_relation": null
-  },
-  {
-    "name": "SPINE_NEXT_STEP_MAX",
-    "site": "src/schema/caps.ts:9",
-    "value": 500,
-    "basis": "unrecorded",
-    "reason": null,
-    "mirrors": null,
-    "mirror_relation": null
-  },
-  {
-    "name": "SPINE_LAST_SESSION_MAX",
-    "site": "src/schema/caps.ts:10",
-    "value": 500,
-    "basis": "unrecorded",
-    "reason": null,
-    "mirrors": null,
-    "mirror_relation": null
-  },
-  {
-    "name": "SPINE_LANDED_MAX",
-    "site": "src/schema/caps.ts:11",
-    "value": 500,
-    "basis": "unrecorded",
-    "reason": null,
-    "mirrors": null,
-    "mirror_relation": null
-  },
   {
     "name": "CRITERIA_MAX_ELEMENTS",
-    "site": "src/schema/caps.ts:13",
+    "site": "src/schema/caps.ts:6",
     "value": 40,
     "basis": "unrecorded",
     "reason": null,
@@ -532,7 +487,7 @@
   },
   {
     "name": "CRITERIA_RETENTION_MAX_ELEMENTS",
-    "site": "src/schema/caps.ts:14",
+    "site": "src/schema/caps.ts:7",
     "value": 200,
     "basis": "unrecorded",
     "reason": null,
@@ -541,7 +496,7 @@
   },
   {
     "name": "CRITERION_TEXT_MAX",
-    "site": "src/schema/caps.ts:15",
+    "site": "src/schema/caps.ts:8",
     "value": 500,
     "basis": "unrecorded",
     "reason": null,
@@ -550,7 +505,7 @@
   },
   {
     "name": "CRITERION_CHECK_MAX",
-    "site": "src/schema/caps.ts:16",
+    "site": "src/schema/caps.ts:9",
     "value": 500,
     "basis": "unrecorded",
     "reason": null,
@@ -559,7 +514,7 @@
   },
   {
     "name": "RISKS_PER_CALL_MAX_ELEMENTS",
-    "site": "src/schema/caps.ts:18",
+    "site": "src/schema/caps.ts:11",
     "value": 40,
     "basis": "unrecorded",
     "reason": null,
@@ -568,7 +523,7 @@
   },
   {
     "name": "RISK_SCOPE_MAX",
-    "site": "src/schema/caps.ts:19",
+    "site": "src/schema/caps.ts:12",
     "value": 200,
     "basis": "unrecorded",
     "reason": null,
@@ -577,7 +532,7 @@
   },
   {
     "name": "RISK_REFS_MAX_ELEMENTS",
-    "site": "src/schema/caps.ts:20",
+    "site": "src/schema/caps.ts:13",
     "value": 10,
     "basis": "unrecorded",
     "reason": null,
@@ -586,7 +541,7 @@
   },
   {
     "name": "RISK_REF_MAX",
-    "site": "src/schema/caps.ts:21",
+    "site": "src/schema/caps.ts:14",
     "value": 200,
     "basis": "unrecorded",
     "reason": null,
@@ -595,7 +550,7 @@
   },
   {
     "name": "KEY_DECISIONS_MAX_ELEMENTS",
-    "site": "src/schema/caps.ts:23",
+    "site": "src/schema/caps.ts:16",
     "value": 200,
     "basis": "unrecorded",
     "reason": null,
@@ -604,7 +559,7 @@
   },
   {
     "name": "KEY_DECISION_TITLE_MAX",
-    "site": "src/schema/caps.ts:24",
+    "site": "src/schema/caps.ts:17",
     "value": 200,
     "basis": "unrecorded",
     "reason": null,
@@ -613,7 +568,7 @@
   },
   {
     "name": "KEY_DECISION_SCOPE_MAX",
-    "site": "src/schema/caps.ts:25",
+    "site": "src/schema/caps.ts:18",
     "value": 200,
     "basis": "unrecorded",
     "reason": null,
@@ -622,7 +577,7 @@
   },
   {
     "name": "OUT_OF_SCOPE_MAX_ELEMENTS",
-    "site": "src/schema/caps.ts:27",
+    "site": "src/schema/caps.ts:20",
     "value": 40,
     "basis": "unrecorded",
     "reason": null,
@@ -631,7 +586,7 @@
   },
   {
     "name": "OUT_OF_SCOPE_TEXT_MAX",
-    "site": "src/schema/caps.ts:28",
+    "site": "src/schema/caps.ts:21",
     "value": 300,
     "basis": "unrecorded",
     "reason": null,
@@ -640,7 +595,7 @@
   },
   {
     "name": "ARTIFACT_LABEL_MAX",
-    "site": "src/schema/caps.ts:30",
+    "site": "src/schema/caps.ts:23",
     "value": 200,
     "basis": "unrecorded",
     "reason": null,
@@ -649,7 +604,7 @@
   },
   {
     "name": "ARTIFACT_POINTER_MAX",
-    "site": "src/schema/caps.ts:31",
+    "site": "src/schema/caps.ts:24",
     "value": 500,
     "basis": "unrecorded",
     "reason": null,
@@ -658,7 +613,7 @@
   },
   {
     "name": "ARTIFACTS_PER_CALL_MAX_ELEMENTS",
-    "site": "src/schema/caps.ts:32",
+    "site": "src/schema/caps.ts:25",
     "value": 40,
     "basis": "unrecorded",
     "reason": null,
@@ -667,7 +622,7 @@
   },
   {
     "name": "DECISION_TITLE_MAX",
-    "site": "src/schema/caps.ts:34",
+    "site": "src/schema/caps.ts:27",
     "value": 200,
     "basis": "unrecorded",
     "reason": null,
@@ -676,7 +631,7 @@
   },
   {
     "name": "DECISION_OPTIONS_MAX_ELEMENTS",
-    "site": "src/schema/caps.ts:35",
+    "site": "src/schema/caps.ts:28",
     "value": 20,
     "basis": "unrecorded",
     "reason": null,
@@ -685,7 +640,7 @@
   },
   {
     "name": "DECISION_OPTION_MAX",
-    "site": "src/schema/caps.ts:36",
+    "site": "src/schema/caps.ts:29",
     "value": 500,
     "basis": "unrecorded",
     "reason": null,
@@ -694,7 +649,7 @@
   },
   {
     "name": "DECISION_SUPERSEDES_MAX_ELEMENTS",
-    "site": "src/schema/caps.ts:37",
+    "site": "src/schema/caps.ts:30",
     "value": 20,
     "basis": "unrecorded",
     "reason": null,
@@ -703,7 +658,7 @@
   },
   {
     "name": "DECISION_COMMIT_MAX",
-    "site": "src/schema/caps.ts:38",
+    "site": "src/schema/caps.ts:31",
     "value": 64,
     "basis": "unrecorded",
     "reason": null,
@@ -712,7 +667,7 @@
   },
   {
     "name": "SESSION_ACTOR_MAX",
-    "site": "src/schema/caps.ts:40",
+    "site": "src/schema/caps.ts:33",
     "value": 100,
     "basis": "unrecorded",
     "reason": null,
@@ -721,7 +676,7 @@
   },
   {
     "name": "SESSION_BODY_MAX",
-    "site": "src/schema/caps.ts:41",
+    "site": "src/schema/caps.ts:34",
     "value": 32000,
     "basis": "chosen",
     "reason": "Ruled by the human operator on 2026-09-09 (decision 01M24GFGJZEQE7JT4VJCRDBQS3). Across twenty-five entries, bodies ran a median of 2669 characters, largest 7814, against a cap of 8000; the largest is 98 percent of the ceiling, a sample censored by its own cap, so the observed maximum is a lower bound on demand rather than a measure of it. 32000 is four times the largest entry anyone has written.",
@@ -730,7 +685,7 @@
   },
   {
     "name": "UNRECOGNIZED_KEYS_SHOWN_MAX",
-    "site": "src/schema/caps.ts:43",
+    "site": "src/schema/caps.ts:36",
     "value": 5,
     "basis": "unrecorded",
     "reason": null,
@@ -739,7 +694,7 @@
   },
   {
     "name": "UNRECOGNIZED_KEY_NAME_MAX",
-    "site": "src/schema/caps.ts:44",
+    "site": "src/schema/caps.ts:37",
     "value": 100,
     "basis": "unrecorded",
     "reason": null,
@@ -748,7 +703,7 @@
   },
   {
     "name": "UNPARSEABLE_RECORDS_SHOWN_MAX",
-    "site": "src/schema/caps.ts:46",
+    "site": "src/schema/caps.ts:39",
     "value": 5,
     "basis": "unrecorded",
     "reason": null,
@@ -757,7 +712,7 @@
   },
   {
     "name": "UNPARSEABLE_RECORD_NAME_MAX",
-    "site": "src/schema/caps.ts:47",
+    "site": "src/schema/caps.ts:40",
     "value": 100,
     "basis": "unrecorded",
     "reason": null,
@@ -865,7 +820,7 @@
   },
   {
     "name": "SESSION_UNPARKED_ENTRIES_MAX",
-    "site": "src/schema/caps.ts:49",
+    "site": "src/schema/caps.ts:42",
     "value": 25,
     "basis": "chosen",
     "reason": "Ruled by the human operator on 2026-09-11 (decision 01M27EQRZQF4R46NWA44Y10ZY0); across the twenty-four historical un-parked runs this store has carried, the largest legitimate run is 24 and the pathological one is 154; 25 refuses exactly that one run, at its 26th append, and leaves the other twenty-three alone; the margin is therefore zero, which is the known cost of the ruling; the bound is a pathology bound, not a page bound, and makes no claim to keep a briefing inside any size.",
diff --git a/src/domain/spine.ts b/src/domain/spine.ts
index c3f345de..b7f73b14 100644
--- a/src/domain/spine.ts
+++ b/src/domain/spine.ts
@@ -13,16 +13,8 @@ export type SpineContribution = {
   out_of_scope?: OutOfScope[]
 }
 
-type ScalarField = { [K in keyof Spine]: Spine[K] extends unknown[] ? never : K }[keyof Spine]
 type CollectionField = { [K in keyof Spine]: Spine[K] extends unknown[] ? K : never }[keyof Spine]
 
-const SCALAR_CAP: Record<ScalarField, number> = {
-  active_goal: caps.SPINE_ACTIVE_GOAL_MAX,
-  next_step: caps.SPINE_NEXT_STEP_MAX,
-  landed: caps.SPINE_LANDED_MAX,
-  last_session: caps.SPINE_LAST_SESSION_MAX
-}
-
 const COLLECTION_ELEMENTS_CAP: Record<CollectionField, number | null> = {
   open_risks: null,
   key_decisions: caps.KEY_DECISIONS_MAX_ELEMENTS,
@@ -35,7 +27,6 @@ const CALLER_FIELD: Record<CollectionField, string> = {
   out_of_scope: 'out_of_scope_add'
 }
 
-const SCALAR_FIELDS: ScalarField[] = Object.keys(SCALAR_CAP) as ScalarField[]
 const COLLECTION_FIELDS: CollectionField[] = Object.keys(COLLECTION_ELEMENTS_CAP) as CollectionField[]
 
 const capRefusal = (field: string, limit: number, observed: number, unit: string, remedy: string): Refusal => ({
@@ -55,13 +46,6 @@ const checkTextCap = (field: string, value: string, limit: number, remedy: strin
   return null
 }
 
-const checkScalarField = (field: ScalarField, value: string | undefined): Refusal | null => {
-  if (value === undefined) {
-    return null
-  }
-  return checkTextCap(field, value, SCALAR_CAP[field], 'shorten the value and retry')
-}
-
 const checkCollectionCount = (field: CollectionField, storedCount: number, contributedCount: number): Refusal | null => {
   const limit = COLLECTION_ELEMENTS_CAP[field]
   if (limit === null) {
@@ -217,12 +201,6 @@ const mergeSpine = (stored: Spine, contribution: SpineContribution): Spine => ({
 })
 
 export const contributeToSpine = (stored: Spine, contribution: SpineContribution): Ok<Spine> | Refusal => {
-  for (const field of SCALAR_FIELDS) {
-    const refusal = checkScalarField(field, contribution[field])
-    if (refusal !== null) {
-      return refusal
-    }
-  }
   for (const field of COLLECTION_FIELDS) {
     const refusal = checkCollectionField(field, stored, contribution)
     if (refusal !== null) {
diff --git a/src/render/briefing.ts b/src/render/briefing.ts
index 22928869..a6e1639c 100644
--- a/src/render/briefing.ts
+++ b/src/render/briefing.ts
@@ -14,17 +14,12 @@ import {
   OUT_OF_SCOPE_TEXT_MAX,
   RISK_REF_MAX,
   SESSION_BODY_MAX,
-  SPINE_ACTIVE_GOAL_MAX,
-  SPINE_LANDED_MAX,
-  SPINE_LAST_SESSION_MAX,
-  SPINE_NEXT_STEP_MAX,
-  THREAD_BLOCKED_BY_MAX,
-  THREAD_SLUG_MAX,
-  THREAD_TITLE_MAX
+  THREAD_SLUG_MAX
 } from '../schema/caps.ts'
 
 const FORMER_RISK_TEXT_MAX = 500
 const FORMER_CRITERION_SETTLED_BY_MAX = 500
+const FORMER_HEADER_FIELD_WRITE_MAX = 500
 
 export type DecisionIntegrity = {
   resolved: number
@@ -79,14 +74,7 @@ export const SESSION_ENTRY_TEXT_FLOOR = 200
 export const ARTIFACT_LABEL_FLOOR = ARTIFACT_LABEL_MAX
 export const ARTIFACT_POINTER_FLOOR = ARTIFACT_POINTER_MAX
 
-const HEADER_FIELD_ESCAPED_GRAPHEME_MAX = Math.max(
-  THREAD_TITLE_MAX,
-  THREAD_BLOCKED_BY_MAX,
-  SPINE_ACTIVE_GOAL_MAX,
-  SPINE_NEXT_STEP_MAX,
-  SPINE_LAST_SESSION_MAX,
-  SPINE_LANDED_MAX
-)
+const HEADER_FIELD_ESCAPED_GRAPHEME_MAX = FORMER_HEADER_FIELD_WRITE_MAX
 
 export const MIN_TEXT_CLIP = CLIP_MARKER_GRAPHEMES
 const NO_CLIP = Number.POSITIVE_INFINITY
@@ -433,7 +421,7 @@ const assembleBriefing = (
   const nextStepLines = thread.spine.next_step.length === 0 ? [] : [thread.spine.next_step]
 
   const headerInlineValues = [thread.title, ...(thread.blocked_by === null ? [] : [thread.blocked_by])]
-  const headerBlockValues = [...activeGoalLines, ...legacyLastSessionText, ...landedLines, ...nextStepLines]
+  const headerBlockValues = [...activeGoalLines, ...legacyLastSessionText, ...landedLines]
   const headerWasShortened =
     headerInlineValues.some(headerInlineWasShortened) ||
     headerBlockValues.some((value) => blockWasShortened(value, HEADER_FIELD_ESCAPED_GRAPHEME_MAX))
@@ -506,7 +494,7 @@ const assembleBriefing = (
     ...nextStepLines.slice(0, 1).map(() => ''),
     ...nextStepLines.slice(0, 1).map(() => '**Next step:**'),
     ...nextStepLines.slice(0, 1).map(() => ''),
-    ...nextStepLines.map((value) => escapeStoredBlock(value, HEADER_FIELD_ESCAPED_GRAPHEME_MAX)),
+    ...nextStepLines.map((value) => escapeStoredBlock(value)),
     ...relatedThreads.slice(0, 1).map(() => ''),
     ...relatedThreads.slice(0, 1).map(() => '**Related:**'),
     ...relatedLines,
diff --git a/src/schema/caps.ts b/src/schema/caps.ts
index 7c824340..dc41f787 100644
--- a/src/schema/caps.ts
+++ b/src/schema/caps.ts
@@ -1,15 +1,8 @@
-export const THREAD_TITLE_MAX = 200
 export const THREAD_SLUG_MAX = 64
-export const THREAD_BLOCKED_BY_MAX = 500
 export const THREAD_CLOSURE_DETAIL_MAX = 2000
 
 export const BINDING_BRANCH_MAX = 255
 
-export const SPINE_ACTIVE_GOAL_MAX = 500
-export const SPINE_NEXT_STEP_MAX = 500
-export const SPINE_LAST_SESSION_MAX = 500
-export const SPINE_LANDED_MAX = 500
-
 export const CRITERIA_MAX_ELEMENTS = 40
 export const CRITERIA_RETENTION_MAX_ELEMENTS = 200
 export const CRITERION_TEXT_MAX = 500
diff --git a/src/schema/thread.ts b/src/schema/thread.ts
index 59e49a81..6065a8e1 100644
--- a/src/schema/thread.ts
+++ b/src/schema/thread.ts
@@ -171,12 +171,10 @@ const ArtifactSchema = structural(
 )
 
 const SpineSchema = z.object({
-  active_goal: content(z.string().max(caps.SPINE_ACTIVE_GOAL_MAX).describe('the thread goal currently being worked')),
-  next_step: content(z.string().max(caps.SPINE_NEXT_STEP_MAX).describe('the next concrete step in this thread')),
-  landed: content(
-    z.string().max(caps.SPINE_LANDED_MAX).describe('what this thread has landed and verified so far, as the previous session left it')
-  ),
-  last_session: content(z.string().max(caps.SPINE_LAST_SESSION_MAX).describe('a summary of the most recent session')),
+  active_goal: content(z.string().describe('the thread goal currently being worked')),
+  next_step: content(z.string().describe('the next concrete step in this thread')),
+  landed: content(z.string().describe('what this thread has landed and verified so far, as the previous session left it')),
+  last_session: content(z.string().describe('a summary of the most recent session')),
   open_risks: z.array(RiskSchema).describe('risks still open on this thread').meta({ class: 'structural' }),
   key_decisions: z
     .array(KeyDecisionSchema)
@@ -195,12 +193,11 @@ const ThreadShape = z.object({
   slug: content(
     z.string().min(1).max(caps.THREAD_SLUG_MAX).regex(SLUG_PATTERN).describe('a short lowercase label for the thread')
   ),
-  title: content(z.string().min(1).max(caps.THREAD_TITLE_MAX).describe('the thread title')),
+  title: content(z.string().min(1).describe('the thread title')),
   status: structural(z.enum(['open', 'done', 'abandoned']).describe('the thread lifecycle state')),
   blocked_by: content(
     z
       .string()
-      .max(caps.THREAD_BLOCKED_BY_MAX)
       .nullable()
       .describe('the reason this thread is blocked, or null when it is not blocked')
   ),
diff --git a/src/server/tools/open_thread.ts b/src/server/tools/open_thread.ts
index b8039d19..13af9e21 100644
--- a/src/server/tools/open_thread.ts
+++ b/src/server/tools/open_thread.ts
@@ -36,7 +36,7 @@ const CriterionCreateSchema = z
   .describe('one completion criterion together with the check that decides it and the settledness that says who stands behind it')
 
 const OpenThreadInputSchema = z.strictObject({
-  title: z.string().min(1).max(caps.THREAD_TITLE_MAX).describe('the one-line thread title'),
+  title: z.string().min(1).describe('the one-line thread title'),
   slug: z
     .string()
     .min(1)
@@ -53,13 +53,13 @@ const OpenThreadInputSchema = z.strictObject({
   active_goal: z
     .string()
     .regex(/\S/)
-    .max(caps.SPINE_ACTIVE_GOAL_MAX)
     .describe('what this thread is trying to achieve, in one or two sentences a fresh session can act on'),
   next_step: z
     .string()
     .regex(/\S/)
-    .max(caps.SPINE_NEXT_STEP_MAX)
-    .describe('the next action someone would take, naming the file and the place in it where the action involves one'),
+    .describe(
+      'the next action someone would take, stated as one decision about what to do next, naming the file and the place in it where the action involves one'
+    ),
   completion_criteria: z
     .array(CriterionCreateSchema)
     .max(caps.CRITERIA_MAX_ELEMENTS)
@@ -121,33 +121,6 @@ export const duplicateSlugRefusal = (slug: string): Refusal => ({
   message: `slug "${slug}" is already used by another thread in this project.`
 })
 
-const titleCapRefusal = (observed: number): Refusal => ({
-  ok: false,
-  field: 'title',
-  accepted: `at most ${caps.THREAD_TITLE_MAX} characters after escaping`,
-  example: 'ship the health check before closing this thread',
-  retryable: true,
-  message: `title exceeds its cap of ${caps.THREAD_TITLE_MAX} characters after escaping; observed ${observed}; remedy: shorten the title and retry.`
-})
-
-const activeGoalCapRefusal = (observed: number): Refusal => ({
-  ok: false,
-  field: 'active_goal',
-  accepted: `at most ${caps.SPINE_ACTIVE_GOAL_MAX} characters after escaping`,
-  example: 'ship the health check before closing this thread',
-  retryable: true,
-  message: `active_goal exceeds its cap of ${caps.SPINE_ACTIVE_GOAL_MAX} characters after escaping; observed ${observed}; remedy: shorten the value and retry.`
-})
-
-const nextStepCapRefusal = (observed: number): Refusal => ({
-  ok: false,
-  field: 'next_step',
-  accepted: `at most ${caps.SPINE_NEXT_STEP_MAX} characters after escaping`,
-  example: 'ship the health check before closing this thread',
-  retryable: true,
-  message: `next_step exceeds its cap of ${caps.SPINE_NEXT_STEP_MAX} characters after escaping; observed ${observed}; remedy: shorten the value and retry.`
-})
-
 const criterionTextCapRefusal = (index: number, observed: number): Refusal => ({
   ok: false,
   field: 'completion_criteria',
@@ -212,19 +185,8 @@ export const openThreadTool: ToolSpec<OpenThreadInput, OpenThreadOutput> = {
     }
 
     const escapedTitle = escapeStored(input.title)
-    if (escapedTitle.length > caps.THREAD_TITLE_MAX) {
-      return { ok: false, refusal: titleCapRefusal(escapedTitle.length) }
-    }
-
     const escapedActiveGoal = escapeStored(input.active_goal)
-    if (escapedActiveGoal.length > caps.SPINE_ACTIVE_GOAL_MAX) {
-      return { ok: false, refusal: activeGoalCapRefusal(escapedActiveGoal.length) }
-    }
-
     const escapedNextStep = escapeStored(input.next_step)
-    if (escapedNextStep.length > caps.SPINE_NEXT_STEP_MAX) {
-      return { ok: false, refusal: nextStepCapRefusal(escapedNextStep.length) }
-    }
 
     const criteria = input.completion_criteria ?? []
 
diff --git a/src/server/tools/park_thread.ts b/src/server/tools/park_thread.ts
index 24d4da64..0ba3f677 100644
--- a/src/server/tools/park_thread.ts
+++ b/src/server/tools/park_thread.ts
@@ -33,12 +33,10 @@ const ParkThreadInputSchema = z.strictObject({
   ).optional(),
   next_step: z
     .string()
-    .max(caps.SPINE_NEXT_STEP_MAX)
     .optional()
-    .describe('replaces the spine next_step field when supplied; omit to leave it unchanged'),
+    .describe('replaces the spine next_step field when supplied, stated as one decision about what to do next; omit to leave it unchanged'),
   landed: z
     .string()
-    .max(caps.SPINE_LANDED_MAX)
     .optional()
     .describe(
       'what this thread has landed and verified, replacing the stored value when supplied; omit to leave it unchanged'
diff --git a/src/server/tools/update_thread.ts b/src/server/tools/update_thread.ts
index aa1dacae..91b46c1f 100644
--- a/src/server/tools/update_thread.ts
+++ b/src/server/tools/update_thread.ts
@@ -91,23 +91,23 @@ const UpdateThreadInputSchema = z.strictObject({
     ),
   active_goal: z
     .string()
-    .max(caps.SPINE_ACTIVE_GOAL_MAX)
     .optional()
-    .describe('replaces the spine active_goal field when supplied; omit to leave it unchanged'),
+    .describe(
+      'replaces the spine active_goal field when supplied, stated as what the thread is trying to achieve in one or two sentences; omit to leave it unchanged'
+    ),
   next_step: z
     .string()
-    .max(caps.SPINE_NEXT_STEP_MAX)
     .optional()
-    .describe('replaces the spine next_step field when supplied; omit to leave it unchanged'),
+    .describe('replaces the spine next_step field when supplied, stated as one decision about what to do next; omit to leave it unchanged'),
   last_session: z
     .string()
-    .max(caps.SPINE_LAST_SESSION_MAX)
     .optional()
-    .describe('replaces the spine last_session field when supplied; omit to leave it unchanged'),
+    .describe(
+      'replaces the spine last_session field when supplied, stated as a short summary of the most recent session; omit to leave it unchanged'
+    ),
   blocked_by: z
     .string()
     .min(1)
-    .max(caps.THREAD_BLOCKED_BY_MAX)
     .optional()
     .describe('what this thread is blocked on; omit to leave it unchanged, and send blocked_by_clear to clear it'),
   blocked_by_clear: z
@@ -295,15 +295,6 @@ export const conflictingBlockageRefusal = (): Refusal => ({
   message: 'blocked_by and blocked_by_clear were both supplied; send one or the other, not both.'
 })
 
-export const blockedByCapRefusal = (observed: number): Refusal => ({
-  ok: false,
-  field: 'blocked_by',
-  accepted: `at most ${caps.THREAD_BLOCKED_BY_MAX} characters after escaping`,
-  example: 'waiting on the infra approval',
-  retryable: true,
-  message: `blocked_by exceeds its cap of ${caps.THREAD_BLOCKED_BY_MAX} characters after escaping; observed ${observed}; remedy: shorten the blocked_by text and retry.`
-})
-
 export const unknownDecisionRefusal = (ids: string[]): Refusal => ({
   ok: false,
   field: 'key_decisions_add',
@@ -539,9 +530,6 @@ export const updateThreadTool: ToolSpec<UpdateThreadInput, UpdateThreadOutput> =
       return { ok: false, refusal: conflictingBlockageRefusal() }
     }
     const escapedBlockedBy = input.blocked_by === undefined ? undefined : escapeStored(input.blocked_by)
-    if (escapedBlockedBy !== undefined && escapedBlockedBy.length > caps.THREAD_BLOCKED_BY_MAX) {
-      return { ok: false, refusal: blockedByCapRefusal(escapedBlockedBy.length) }
-    }
     const blockageChanged = blockedBySupplied || blockedByCleared
 
     const nothingChanged =
diff --git a/test/contract/no-path.test.ts b/test/contract/no-path.test.ts
index 013c24b1..4589d439 100644
--- a/test/contract/no-path.test.ts
+++ b/test/contract/no-path.test.ts
@@ -85,7 +85,6 @@ const UPDATE_THREAD_UNKNOWN_CRITERION_PRODUCER: ProducerId = 'server/tools/updat
 const UPDATE_THREAD_UNKNOWN_DECISION_PRODUCER: ProducerId = 'server/tools/update_thread.ts#unknownDecisionRefusal'
 const UPDATE_THREAD_CONFLICTING_BLOCKAGE_PRODUCER: ProducerId =
   'server/tools/update_thread.ts#conflictingBlockageRefusal'
-const UPDATE_THREAD_BLOCKED_BY_CAP_PRODUCER: ProducerId = 'server/tools/update_thread.ts#blockedByCapRefusal'
 const CLOSE_THREAD_INVALID_THREAD_RECORD_PRODUCER: ProducerId = 'server/tools/close_thread.ts#invalidThreadRecordRefusal'
 const CLOSE_THREAD_COMMIT_FAILURE_PRODUCER: ProducerId = 'server/tools/close_thread.ts#commitFailureRefusal'
 const BIND_BRANCH_COMMIT_FAILURE_PRODUCER: ProducerId = 'server/tools/bind_branch.ts#commitFailureRefusal'
@@ -284,18 +283,6 @@ const collectToolRefusals = async (): Promise<TaggedRefusal[]> => {
       refusal: conflictingBlockage.refusal
     })
 
-    const overflowingBlockedBy = await updateThreadTool.handler(rt, STUB_TOOL_CTX, {
-      thread_id: threadId,
-      blocked_by: CONTROL_CHAR_OVERFLOW(84)
-    })
-    if (overflowingBlockedBy.ok) {
-      throw new Error('expected updateThreadTool to refuse a blocked_by whose escaped form overflows its cap')
-    }
-    refusals.push({
-      producer: UPDATE_THREAD_BLOCKED_BY_CAP_PRODUCER,
-      refusal: overflowingBlockedBy.refusal
-    })
-
     const missingKind = await amendCriteriaTool.handler(rt, STUB_TOOL_CTX, {
       thread_id: threadId,
       operation: 'insert',
@@ -1226,9 +1213,14 @@ const collectRealRefusals = async (): Promise<TaggedRefusal[]> => {
   refusals.push({ producer: STRIKE_CRITERION_PRODUCER, refusal: strikeResult })
 
   const spineResult = contributeToSpine(domainThread.spine, {
-    active_goal: 'a'.repeat(caps.SPINE_ACTIVE_GOAL_MAX + 1)
+    key_decisions: Array.from({ length: caps.KEY_DECISIONS_MAX_ELEMENTS + 1 }, (_, index) => ({
+      id: domainRt.ulid(),
+      decision_id: domainRt.ulid(),
+      title: `census decision ${index}`,
+      scope: 'census scope'
+    }))
   })
-  if (spineResult.ok) throw new Error('expected contributeToSpine to refuse on an oversized active_goal')
+  if (spineResult.ok) throw new Error('expected contributeToSpine to refuse key decisions past their element cap')
   refusals.push({ producer: CONTRIBUTE_TO_SPINE_PRODUCER, refusal: spineResult })
 
   const transitionResult = transition(domainRt, domainThread, 'abandoned', '')
diff --git a/test/support/briefing-sweep-fixture.ts b/test/support/briefing-sweep-fixture.ts
index f678dcfc..efe487c5 100644
--- a/test/support/briefing-sweep-fixture.ts
+++ b/test/support/briefing-sweep-fixture.ts
@@ -14,6 +14,8 @@ export type SweepShape = {
 
 export type SweepFixture = { thread: Thread; predecessor: Thread; integrity: DecisionIntegrity }
 
+const FORMER_THREAD_TITLE_MAX = 200
+const FORMER_HEADER_TEXT_MAX = 500
 const CRITERION_CHECK_HELD_LENGTH = 40
 const CRITERION_RESULT_HELD_LENGTH = 60
 const RISK_REF_HELD_LENGTH = 40
@@ -84,7 +86,7 @@ export const buildSweepFixture = (rt: Runtime, shape: SweepShape): SweepFixture
   const predecessor: Thread = {
     id: rt.ulid(),
     slug: 'p'.repeat(caps.THREAD_SLUG_MAX),
-    title: fillOf(caps.THREAD_TITLE_MAX),
+    title: fillOf(FORMER_THREAD_TITLE_MAX),
     status: 'done',
     blocked_by: null,
     completion_criteria: [],
@@ -104,16 +106,16 @@ export const buildSweepFixture = (rt: Runtime, shape: SweepShape): SweepFixture
   const thread: Thread = {
     id: rt.ulid(),
     slug: 's'.repeat(caps.THREAD_SLUG_MAX),
-    title: fillOf(caps.THREAD_TITLE_MAX),
+    title: fillOf(FORMER_THREAD_TITLE_MAX),
     status: 'open',
-    blocked_by: fillOf(caps.THREAD_BLOCKED_BY_MAX),
+    blocked_by: fillOf(FORMER_HEADER_TEXT_MAX),
     predecessor_id: predecessor.id,
     completion_criteria: criteria,
     spine: {
-      active_goal: fillOf(caps.SPINE_ACTIVE_GOAL_MAX),
-      next_step: fillOf(caps.SPINE_NEXT_STEP_MAX),
+      active_goal: fillOf(FORMER_HEADER_TEXT_MAX),
+      next_step: fillOf(FORMER_HEADER_TEXT_MAX),
       landed: '',
-      last_session: fillOf(caps.SPINE_LAST_SESSION_MAX),
+      last_session: fillOf(FORMER_HEADER_TEXT_MAX),
       open_risks: risks,
       key_decisions: keyDecisions,
       out_of_scope: outOfScope
diff --git a/test/unit/briefing-floors.test.ts b/test/unit/briefing-floors.test.ts
index ab5368d8..fca11a5e 100644
--- a/test/unit/briefing-floors.test.ts
+++ b/test/unit/briefing-floors.test.ts
@@ -25,7 +25,6 @@ import { ThreadRecord, type Thread, type Criterion, type Risk, type KeyDecision,
 import { SessionRecord, type SessionEntry } from '../../src/schema/session.ts'
 import { itemCountOverBudgetThread } from '../support/briefing-item-count-over-budget-fixture.ts'
 import {
-  THREAD_TITLE_MAX,
   THREAD_SLUG_MAX,
   RISK_REF_MAX,
   KEY_DECISION_TITLE_MAX,
@@ -38,6 +37,7 @@ import {
 } from '../../src/schema/caps.ts'
 import { testRuntime } from '../support/runtime.ts'
 
+const FORMER_THREAD_TITLE_MAX = 200
 const FORMER_RISK_TEXT_MAX = 500
 const FORMER_CRITERION_RESULT_MAX = 1000
 const FORMER_CRITERION_SETTLED_BY_MAX = 500
@@ -136,7 +136,7 @@ const SLUG_SAFE_FILL_CHAR = 'a'
 const slugSafeFillFor = (floor: number, cap: number): string => SLUG_SAFE_FILL_CHAR.repeat(Math.min(floor, cap))
 
 const buildFloorFixture = (rawTextFor: FloorRawText, sessionBodies: readonly string[]): Fixture => {
-  const relatedTitleText = rawTextFor(RELATED_TITLE_FLOOR, THREAD_TITLE_MAX)
+  const relatedTitleText = rawTextFor(RELATED_TITLE_FLOOR, FORMER_THREAD_TITLE_MAX)
   const relatedSlugText = slugSafeFillFor(RELATED_SLUG_FLOOR, THREAD_SLUG_MAX)
   const riskText = rawTextFor(RISK_TEXT_FLOOR, FORMER_RISK_TEXT_MAX)
   const settledRiskText = rawTextFor(RISK_TEXT_FLOOR, FORMER_RISK_TEXT_MAX)
@@ -684,7 +684,7 @@ test('briefing.a-clip-floor-clamped-field-does-not-trip-the-shortened-text-bulle
 
 test('briefing.every-floor-is-at-most-the-write-cap-it-governs', () => {
   const pairs: Array<{ name: string; floor: number; cap: number }> = [
-    { name: 'related title floor vs thread title cap', floor: RELATED_TITLE_FLOOR, cap: THREAD_TITLE_MAX },
+    { name: 'related title floor vs former thread title cap', floor: RELATED_TITLE_FLOOR, cap: FORMER_THREAD_TITLE_MAX },
     { name: 'related slug floor vs thread slug cap', floor: RELATED_SLUG_FLOOR, cap: THREAD_SLUG_MAX },
     { name: 'risk text floor vs risk text cap', floor: RISK_TEXT_FLOOR, cap: FORMER_RISK_TEXT_MAX },
     { name: 'risk reference floor vs risk reference cap', floor: RISK_REF_FLOOR, cap: RISK_REF_MAX },
diff --git a/test/unit/briefing-hides-nothing.test.ts b/test/unit/briefing-hides-nothing.test.ts
index 65c79fd7..9b301ba8 100644
--- a/test/unit/briefing-hides-nothing.test.ts
+++ b/test/unit/briefing-hides-nothing.test.ts
@@ -15,6 +15,8 @@ import { REBUILD_ROOT, forEachDescendant, lineOf, loadSourceProgram, relativeToR
 
 const rt = testRuntime()
 
+const FORMER_THREAD_TITLE_MAX = 200
+
 const EMPTY_INTEGRITY: DecisionIntegrity = { resolved: 0, dangling: [], quarantined: [] }
 
 type SliceSite = { file: string; line: number; expression: string; discardsElements: boolean }
@@ -255,7 +257,7 @@ const threadOf = (overrides: Partial<Thread> = {}): Thread => ({
 })
 
 test('briefing.a-render-that-fits-its-budget-is-clipped-nowhere', () => {
-  const predecessor = threadOf({ title: ESCAPE_EXPANDING_CHAR.repeat(caps.THREAD_TITLE_MAX), status: 'done' })
+  const predecessor = threadOf({ title: ESCAPE_EXPANDING_CHAR.repeat(FORMER_THREAD_TITLE_MAX), status: 'done' })
   const thread = threadOf({
     predecessor_id: predecessor.id,
     completion_criteria: [
diff --git a/test/unit/session-start.test.ts b/test/unit/session-start.test.ts
index cf0eac76..dde14ab0 100644
--- a/test/unit/session-start.test.ts
+++ b/test/unit/session-start.test.ts
@@ -96,8 +96,10 @@ test('session-start.roster-line-still-carries-slug-title-next-step-and-id', () =
 })
 
 const LONG_THREAD_COUNT = 16
-const LONG_TITLE = 'a'.repeat(caps.THREAD_TITLE_MAX)
-const LONG_NEXT_STEP = 'b'.repeat(caps.SPINE_NEXT_STEP_MAX)
+const FORMER_THREAD_TITLE_MAX = 200
+const FORMER_SPINE_NEXT_STEP_MAX = 500
+const LONG_TITLE = 'a'.repeat(FORMER_THREAD_TITLE_MAX)
+const LONG_NEXT_STEP = 'b'.repeat(FORMER_SPINE_NEXT_STEP_MAX)
 
 const makeLongThread = (rt: Runtime, index: number): Extract<RecordChange, { kind: 'thread' }> => ({
   kind: 'thread',
